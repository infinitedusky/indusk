# PostHog remote MCP server (https://posthog.com/docs/model-context-protocol)
# POSTHOG_MCP_API_KEY: a phx_ PERSONAL API key (Settings > Personal API Keys).
# The MCP accepts Bearer personal-key auth; the key's project access decides
# which projects the tools can see. NOT the phc_ project token.
POSTHOG_MCP_URL=https://mcp.posthog.com/mcp
POSTHOG_MCP_API_KEY=

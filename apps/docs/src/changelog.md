# Changelog

All notable changes to InDusk MCP are documented here. Follows [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

## [Unreleased]

### Added
- **`pnpm wt main` — trunks route from config, never from scanning.** `main` (or `<repo>/main` when several repos are declared, refusing unqualified exactly like an ambiguous slug) and a declared repo name now resolve through the declared layout: the workbench-side `path` when it exists (trunk symlink or nested checkout), else `<repos_root>/<name>`. A sibling-layout workbench whose trunk link was never made had a config that said exactly where the trunk was, and `wt` could not find it — trunk addressing only worked when a root symlink happened to exist.

### Fixed
- **`pnpm wt:pm2` resolution is wt.sh's again, by construction.** It carried its own root-only copy whose header claimed "resolution matches wt.sh" — false since declared layouts shipped in 1.37.0: a worktree in a declared `worktrees` directory was invisible to it. Both scripts now call one `_wt_resolve_target` in `workbench-helpers.sh`, so declared directories, `<repo>/<slug>` disambiguation, and the new trunk targets work identically in both. The bash reserved-name set also picks up `docs`, which both copies had drifted from `RESERVED_ROOT_DIRS`.

## [1.41.0] — 2026-08-30

### Added
- **`indusk context check-pointers` refuses hand-copied version claims in CLAUDE.md.** A `**Version**:` line carrying a literal semver fails when it doesn't match `package.json` — or when there is no `package.json` version to check it against (monorepo roots). dusk's CLAUDE.md said "1.36.0 published" for twelve days and four releases while npm served 1.40.3; nothing in the release flow touches CLAUDE.md, so the only safe version line is a pointer. A matching literal passes today and fails at the next bump — the check catches the copy exactly when it becomes a lie.

### Changed
- **The `context` skill is now `claude-md`.** Claude Code ships a built-in `/context` command (the context-window usage view), and a built-in shadows any skill of the same name — `/context learn "…"` opened the token chart instead of running the skill. Only the name and invocation changed (`/claude-md learn "…"`); the `#### Phase N Context` gate, the `indusk context` CLI, and `get_context` / `update_context` keep their names. `init` and `update` remove the stale `.claude/skills/context/` directory through the new `LEGACY_SKILLS` migration (`lib/skill-migration.ts`, the third twin after retired MCP servers and hooks). It is fingerprint-checked: a user's own skill that happens to be named `context` is reported and left alone.


## [1.40.3] — 2026-08-29

### Fixed
- **A library is no longer asked for telemetry it should not emit.** dusk declares `otel.role: "library"` and correctly ships no instrumentation, and its otel health checks were permanently red demanding some — the same class as the Doppler check that was red on every project without Doppler. Two causes: `otel.role` was consulted for impl gates but never for enablement, and otel's `detect` pattern `**/instrumentation.{ts,py}` matched a **packaged template** rather than real instrumentation. Detect now takes an `exclude` list (templates, `node_modules`), auto-enable skips otel when the role is not `service`, and an already-enabled otel on a non-service project reports **"not applicable — otel.role is …"** with the disable command, rather than failing two checks forever.

## [1.40.2] — 2026-08-29

### Fixed
- **`verification` entries in a `manifest.local.json` merge by name, like health checks do.** They were replaced wholesale, so an override wanting to fix one command had to restate every other one — and then silently froze them at that day's values. That is the forking the override mechanism exists to avoid, reintroduced one key deeper. Both named-entry arrays now merge the same way; anything else in `provides` is still replaced wholesale, which the spec now states.

## [1.40.1] — 2026-08-28

1.40.0 shipped an override mechanism whose file could not be committed. This makes it usable.

### Fixed
- **`manifest.local.json` is trackable, and so is `.env.example`.** Two ordering bugs met in the same block. The blanket `.indusk/extensions/` rule excluded the directory, and **git does not descend into an excluded directory** — so every file negation inside it was a dead letter and `.env.example` had never been tracked on any machine, in any project. Then 1.39.0 appended `.env.*` *after* the existing negation, so even with the directory un-excluded the last rule won. `indusk update` now normalizes the whole block instead of appending to it: secrets ignored, negation last, manifests and `.env.example` tracked.
- **A malformed `manifest.local.json` no longer takes `indusk update` down.** It threw all the way out, with a stack trace, over one bad file — stopping every other extension from updating and burying the message. It now names the file, falls back to the built-in manifest for that extension only, continues, and still fails the exit code. Loud and specific, not catastrophic.

## [1.40.0] — 2026-08-28

### Added
- **`manifest.local.json` — override a shipped extension manifest without forking it.** `.indusk/extensions/` is package-owned: `update` flat-copies the built-in over yours on any hash mismatch, so a hand-edited `manifest.json` is a fork parked in a directory whose purpose is to be replaced, silently reverted on every update. Local changes now go in a separate file that `update` never writes, merged over the built-in at load. **Health checks merge by name**, so overriding one check does not fork the rest and upstream improvements to the others keep arriving.

  This is deliberately *not* a preserve-local-edits rule in `update`. Preserving edits pins a project to a stale fork and hides every upstream fix behind it — trading a loud-once problem for a silent-forever one. Observed in practice: a project forked the otel manifest, a later release improved that same manifest, and the fork would have masked the improvement indefinitely.

  `extensions status` marks an overridden extension with `[manifest.local.json applied]`, and a malformed override **throws** rather than falling back to the built-in — degrading silently would restore the silence this removes.

## [1.39.1] — 2026-08-28

### Fixed
- **`indusk update` told a nested workbench its repo was missing, and to run a command that would have duplicated it.** The materialization nudge read only the legacy `sibling_parent`, never resolved a relative value, and looked repos up by `name` rather than declared `path` — so on a workbench using `repos_root: "."` it reported every repo unmaterialized and recommended `workbench restore`, which would have cloned a second copy beside the workbench and relinked the trunks at it. **The first defect in this family to recommend a destructive action.** `workbench status` had it right the whole time; two code paths, different path logic, one wrong.
- **`repos_root` resolution has one definition.** It had four — `workbench restore`, the health-check runner, doppler's env-pull, and the nudge above — and they disagreed. `resolveReposRoot` is now the single definition, pinned by a test, with the test lane excluded from its own scan because an enforcement grep that matches its own pattern literal is green or red for reasons unrelated to the code.
- **otel health checks find Python.** `python -c "import opentelemetry"` exits 127 on a machine with only `python3`, which is indistinguishable from "package missing" in a boolean result. Tries `python3` first, and `instrumentation.py` is recognised at `src/` as well as the root.
- **`indusk update` retires the blanket `.indusk/extensions/` ignore rule** on projects that still carry it. 1.39.0 added the narrower secrets rules but left the blanket line standing in existing projects, which did nothing — manifests stayed untracked and update kept replacing local fixes silently.

## [1.39.0] — 2026-08-28

Four defects from a report written while building a polyglot workbench. Three
share a root cause — **tooling resolves paths from "the project root", which in
a workbench is the wrapper, where there is no code.**

### Fixed
- **Extension health checks run where the code is.** A check is a shell string in a manifest (`test -f instrumentation.py`), and it ran with cwd at the project root — the wrapper — so in a workbench it could **never** pass, before the work or after it. Fixed in the runner rather than the manifests: each check now runs once per declared repo with cwd set there, so the shell stays naive and becomes correct and every shipped manifest is fixed without being edited. Passes if any repo satisfies it; **does not** pass when nothing does anywhere, because a scope fix that goes green immediately is a relaxation in disguise.
- **`indusk init-docs` scaffolds into the application repo.** Run from a workbench it created `apps/<wrapper-name>-docs/` **in the wrapper** — docs describing the application, orphaned the moment the application is cloned standalone, and named after the wrapper. Now targets the single declared repo and writes `apps/docs/`; with several declared repos it refuses and says to run it from the one the docs belong to.
- **`doppler.apps[].path` means one thing.** Two callers resolved it against different roots — manual `env-pull` against the workbench, worktree auto-provisioning against the worktree — so `path: "app/backend"` made the manual pull work and every worktree silently get nothing. It is now relative to the **application repo** in both.
- **`env-pull` fails when it wrote no files.** It printed `wrote 0 file(s)` and exited 0, and worktree provisioning printed `auto-provisioned env` on top of it. A developer saw two success lines and had no env. A check must distinguish "nothing to do" from "did not run".
- **`indusk init` no longer guesses vitest.** `detected.testRunner ?? "vitest"` asserted vitest wherever detection found nothing — and detection is JS-only, so every Python project was told it used vitest. `verify.testRunner` is now omitted when nothing is detected, with a warning naming what detection looks for.

### Changed
- **Extension manifests are tracked, not gitignored.** `.indusk/extensions/` was ignored wholesale, so a patched manifest was untracked: no diff, no history, and `indusk update` replaced it with no message. The revert was undetectable except by re-running the check it affected — which is how it was found. Only `.env` files are ignored now; manifests are configuration and belong in version control.

## [1.38.3] — 2026-08-27

### Fixed
- **The Doppler health check asks whether Doppler works, not whether a file exists.** `doppler-token-present` tested for a service-token file, so anyone using `doppler login` — the normal path — got a red check on a setup that pulls env perfectly well. The extension's own skill documented the false negative rather than fixing it. Now `doppler-authenticated`: `doppler me`, falling back to the token file for non-interactive contexts.

## [1.38.2] — 2026-08-27

### Changed
- **Doppler is no longer required-by-default.** `required: true` bypasses an extension's own `detect` rule, so doppler enabled on every project regardless of whether it was configured — then its token check hard-errored demanding a credential the project had no use for. Its `detect` (`.indusk/extensions/doppler/.env`) already asked exactly the right question. The cost was not noise: a health check that is permanently red on every project stops being read, which is worse than not having it. `local-telemetry` remains required because InDusk ships that daemon itself; a test now pins the required set so adding to it is a deliberate edit.

### Fixed
- **`workbench restore` clears a dangling link at the clone target.** A workbench created on another machine arrives with `<repo> -> ../<repo>` pointing at a path that does not exist here. `existsSync` follows the link and reports absent, so restore decided to clone — and git refused with `File exists` for a path every check had seen as empty. Bites the nested layout specifically, where the clone target and the trunk path are the same.

## [1.38.1] — 2026-08-27

### Fixed
- **`worktree create` worked in TypeScript and died in bash.** 1.38.0 taught the TS lane `repos_root` and left the shell scripts reading `sibling_parent` only, so a workbench using the new key failed with `missing worktree.sibling_parent` — a two-lane contract broken on one side. `_read_repos_root` is now the bash port, marked as one.
- **Worktree provisioning ran in the wrong directory.** `worktreeDir` assumed the flat layout, so doppler provisioning and every `post_create` command ran with a cwd that did not exist on a declared layout. The worktree itself was created correctly, which is what made it look like the commands were at fault.
- **A nested repo is no longer reported as a problem.** `repos_root: "."` puts the repo at its trunk path deliberately; restore called it a collision and `worktree list` called it "unusual for a workbench trunk".

### Changed
- **Agent conduct gains a Brevity section.** Scaffolded into every project's `AGENTS.md`: lead with the answer, no preamble or process narration, length needs a reason.

## [1.38.0] — 2026-08-27

### Added
- **`repos_root` — where a workbench's repos live, expressible relative to the workbench.** Supersedes `sibling_parent`, which named a *relationship* ("the parent of the siblings") rather than the value, and stopped being true the moment the repos lived inside the workbench. The old key is still read, so no existing workbench needs an edit. A **relative** value resolves against the workbench, which is the point: `repos_root: "."` means "the repos live here" and means the same thing on every machine, so the layout reproduces when the workbench is cloned. An absolute value keeps its old meaning and its old problem — it names whichever machine wrote it — and still falls back loudly where it does not resolve. Demonstrated before the change: a nested workbench cloned to a second machine fell back to the parent and rebuilt itself as the *sibling* layout instead.
- **Layout values may be nested.** `path` and `worktrees` accept a relative path of any depth (`worktrees/alpha`, `repos`), not just one segment. "Cannot escape the workbench" and "must be one segment" were never the same requirement; the guard now rejects absolute paths, `~`, and `..` in **any** position, plus `.git`/`.indusk`/`.claude` as the first segment — `.git/x` is a perfectly clean relative path and still catastrophic.
- **`pnpm wt <slug>` searches declared worktree locations.** `wt.sh` scanned the workbench root and nothing else, so the moment a repo declared `worktrees`, its worktrees were one level down and **invisible** — not ambiguous, absent, reported as "no worktree or trunk matching slug". Declared layouts shipped in 1.37.0 and this was broken by them from the start: the execution surface was never taught the layout the config had learned to express. An ambiguous slug now refuses and names the candidates in the form that resolves them, and **`wt <repo>/<slug>`** disambiguates — repo-qualified rather than directory-qualified, because the repo and the slug are what a person knows while the directory is a config detail that changes when the layout does.

## [1.37.2] — 2026-08-27

### Fixed
- **`workbench migrate-layout` now sticks on the workbenches it was built for.** The migration moves a flat workbench's worktrees under `<repo>-worktrees/` and then declares where they went, so the layout holds. The declaration step iterated `cfg.worktree.repos` — a key a **legacy `wrapped_repo` config does not have** — so on exactly the workbenches this migration exists for, the moves happened and nothing was recorded. It now materializes the singular shape into `repos[]` first, taking the repo set from `readWorkbenchRepos` rather than re-reading the singular field, so the reduction keeps its one definition. Two visible consequences fixed: `worktree list` filed the moved worktrees under **Unattributed** instead of under their repo, and the layout silently undid itself as new worktrees returned to the root.
- **`worktree create <slug>` respects a declared worktrees location on a single-repo workbench.** The location was only passed when the repo was named explicitly, and on a one-repo workbench nobody names it — so every new worktree landed at the workbench root regardless of what the config declared. Resolution now lives in a pure `resolveCreateTarget`, which picks the only declared repo when none is named and still refuses to guess when several are declared.

Both were found by running `migrate-layout` against a copy of a real legacy workbench's config. Every fixture in the suite used the plural shape, so nothing covered the case the feature was built for.

## [1.37.1] — 2026-08-27

### Fixed
- **Two shipped skills could never be listed.** `eval-review` and `toolbelt` had no YAML frontmatter at all — no `---` block, no `name`, no `description` — so Claude Code skipped them silently. They installed correctly, synced correctly, and were simply invisible: no error anywhere, the only symptom a skill nobody can invoke. Present in every release that shipped them, not a 1.37.0 regression. Both now declare `name` + `description`, and a new test asserts every `skills/*.md` carries a frontmatter block whose `name` matches its filename. The existing parity test could not catch this by construction — it compares the package copy against the installed copy, and two identical unregistrable files are perfectly in parity. Byte-equality answers "did it sync", not "does it work".

## [1.37.0] — 2026-08-27

### Added — versioned workbench

- **Workbenches wrap multiple repos.** `worktree.repos[]` in `.indusk/config.json`; the legacy `wrapped_repo` reduces to a one-element list, so existing workbenches need no edit. `indusk worktree create [repo] <slug>` takes the repo argument again, optional when only one is declared.
- **`indusk workbench restore`** materializes a cloned workbench — clones every declared repo as a sibling, recreates the trunk links, and prints the out-of-band set it cannot supply. Idempotent; a partial restore exits non-zero naming what it could not do.
- **`indusk workbench sync`** shares a workbench between machines: commit → pull → push, resolved blindly (`merge=union` on `current.md` and `highlights.jsonl`). Triggered by a debounced hook and at `/catchup`, inert outside a workbench, and never blocking — offline commits locally and goes out on the next sync. The wrapped repos are never auto-committed.
- **`indusk workbench status`** shows, per repo, whether its commits have actually left this machine.
- **Layout is declared, not inferred.** Optional `path` and `worktrees` per repo; absence means today's flat layout. `indusk workbench migrate-layout` moves an existing flat workbench, dry-run by default.

- **Retired hooks are now removed from consumers, not just from the package** (`LEGACY_HOOKS`). Deleting a hook from the package source did nothing to projects that had already installed it: the file stayed in `.claude/hooks/` *and* stayed registered in `.claude/settings.json`, so it kept being invoked forever — or, once someone deleted the file by hand, the registration kept invoking a script that no longer existed. `check-plan-order.js` had been in exactly that state since the context-beam cleanup. `init` and `update` now remove both halves, mirroring the `LEGACY_MCP_SERVERS` migration; future retirements extend the list rather than hand-rolling a loop. A matcher entry whose hooks all disappear is dropped rather than left as an empty shell that matches tool calls and runs nothing.

### Changed

- `indusk worktree list` renders one block per declared repo, groups worktrees structurally, and shows anything it cannot place as **Unattributed** rather than dropping it.
- `indusk update` notices declared repos that are not materialized and points at `workbench restore` without cloning anything itself.

### Fixed

- `indusk verify` no longer reports honestly-done work as phantom inside a workbench. Plan documents and code live in different repositories there, so it refuses rather than judging code by a diff that cannot contain it.

- **`indusk init` no longer fails when a telemetry daemon is already running — a regression shipped in 1.36.2.** The port guard added in that release was applied to *every* start path, including the opportunistic auto-start that fires when `init` enables the `local-telemetry` extension. On any machine already running a daemon — which is most machines that use InDusk — `init` printed `Project registered but daemon failed to start` and the project got no `jaeger` MCP entry. The guard is right where a caller **named** a port and silently binding another is the bug; it is wrong for an auto-start that never asked for `:4318`. It is now scoped to explicit `indusk telemetry start`, and the auto-start bumps as before. Found by the telemetry suite, which encoded the correct behaviour and went red — nine tests across five files, all of which I initially mistook for tests needing updating rather than a regression they had caught.

## [1.36.2] — 2026-08-17

### Added
- **`indusk telemetry reap` — kill telemetry processes whose config no longer exists.** The daemon is spawned `detached` so it survives its terminal, and its PIDs are recorded only in `$INDUSK_HOME/telemetry.json`; anything that deletes that home severs the only handle on the processes. The orphan test is the one the bug reporter identified and it is exact: a live daemon's `--config` path sits inside its live home, an orphan's points into a deleted directory. Protects any PID the current registry claims, strips jaeger's `file:` scheme before testing the path, treats a process with no `--config` as *not* an orphan (absence of evidence — killing something unidentifiable is worse than leaving it), and offers `--dry-run`. Tested against real spawned processes rather than a mocked `ps`, because a mock would only prove the regex matches the string the author chose to feed it — the exact failure mode of the audit that motivated this release.
- **`indusk telemetry start` now refuses to bind a different port than you asked for.** `findFreePort` silently falls back to a random high port when the requested one is taken, and that silence is the whole bug: a leaked jaeger held `:4318`, the real daemon came up elsewhere, and a dev stack configured for the canonical port kept exporting into the stray — which answered **404**, because it is not an OTLP receiver. Every trace vanished while every component reported healthy. Start now fails with the occupied port named and three ways out (`reap`, `--allow-port-bump`, explicit ports). It deliberately does **not** adopt the listener: without its PIDs the daemon cannot manage it, so claiming to reuse it would be a lie. Port `0` still means "any" and is exempt.

### Fixed
- **The test suite no longer leaks a telemetry daemon pair per CLI invocation.** Running any command with `INDUSK_HOME` pointed at a temp directory auto-enables the `local-telemetry` extension, whose `on_enable` hook starts a **detached** jaeger + otelcol pair. The PIDs are recorded only in `$INDUSK_HOME/telemetry.json`, so deleting the temp home destroyed the only record of them and the pair survived unreapable. Three suites did exactly that, leaking ~18 processes per full run; on the machine that surfaced this it had reached **2,058 orphaned processes holding 17.1 GB**, the oldest 16 days old. The second-order damage was worse than the memory: the strays bind randomized high ports and had taken the port the dev stack was configured to export to, so `POST /v1/traces` answered **404** and every trace silently vanished — the classic "Jaeger is up, why are there no traces." Fixed by stopping the daemon through the CLI *before* the temp home is removed (`stopTelemetryForHome`), which also cleans up the registry files. Verified by measurement rather than inspection: the full `indusk-mcp` suite now runs 1006 tests with the process count unchanged, 2 → 2. **Note the mitigation that caused it** — those suites redirect `INDUSK_HOME` specifically so daemon *files* don't leak, and that redirect is what makes the *processes* unreapable. Doing nothing would have leaked less.
- **`check_health` no longer reports a dead telemetry daemon as running.** The `telemetry-daemon-running` check tested `existsSync(~/.indusk/telemetry.pid)` — a file, not a process. A registry pointing at PIDs that had been dead for three weeks reported `ok` while its sibling `telemetry-otlp-listening` reported `error` against the same daemon, a contradiction the check could not express. It now reads `telemetry.json` and verifies both PIDs are alive via `process.kill(pid, 0)`; combined with the sibling port probe, the pair covers what `daemonStatus()`'s identity gate already required internally. (`daemonStatus()` itself was never the naive one — it has always verified PID liveness *and* port-listening.)

## [1.36.1] — 2026-08-16

### Added
- **The Shape check — craft feedback in the phase that wrote the code** (`lifecycle-rebalance`). Until now the only craft review in the lifecycle was `/cleanup` at plan close, which is correct for structural decomposition and far too late for the ordinary case of "this inline block wanted a name." `dawn-verify` is the evidence: report rendering written inline at Phase 2 surfaced at **Phase 7**, where it had to be extracted before the fix could be tested at all. `/work`'s per-phase order now runs a **Shape** step between Verification and Context — the agent reviews the files *that phase* changed against the craft rules of the project's **enabled extensions**, and findings append as ordinary unchecked implementation items to the same phase, so existing gate machinery makes them non-ignorable without Shape blocking anything. It ships as **executor behavior, not plan structure**: no `#### Phase N Shape` heading, no validator rule, and nothing to retrofit into the 51 existing impls — deliberate, because gate vocabulary is defined in four independent places and an unrecognized `####` heading *fails silently*, misclassifying its items rather than erroring. **The executing agent performs the judgment at no extra model call** — it is already a model, and extension craft rules are prose, which a heuristic cannot read; a line-count threshold would have missed the motivating case entirely (about fifteen lines, no threshold crossed). Rules are collected from every enabled extension with no domain-vs-tool filter, since manifests carry no such taxonomy and inventing one in core would be exactly the hardcoded judgment the design forbids. **Three outcomes, never silence** — reviewed-with-findings, reviewed-nothing-found (recorded already-checked, so it stays cheap and common), and skipped-with-reason — because a check that cannot distinguish "nothing to do" from "did not run" reports the shape of success without doing the work; a per-file "reviewed, left as-is" carries its reasoning. Scope is pinned from **both** sides in the suite: Shape declines cross-file duplication, *and* `/cleanup`'s changed-file scan still returns those files, so "Shape reviewed it" can never come to mean nobody looks again. `/cleanup`'s stated scope narrows to inter-file structure. Shape runs only after Verification is green — restructuring code whose correctness is unproven is how a refactor hides a bug. Judgment quality has **no test and cannot have one**, so it is calibrated by observation instead: every retrospective now records the plan's finding and false-positive counts (required even when zero), and two consecutive plans of human-judged-wrong findings reopens calibration as a falsification hypothesis. Claude Code lane only — `atdawn run` gets Shape in a later plan, where the extra model call is a real cost to be paid deliberately.
- **Test authoring is a phase.** `### Test Phase N` alongside `### Build Phase N` as two independently-numbered sequences, ordered by document position. Motivated by measurement rather than taste: across 52 impls, **260 of 444 trajectory rows are `Writable at: Phase 0`**, and Gate A compared `row.writableAt === advancingPhase` where the advancing phase is never 0 — the default path of the system's central discipline was unenforceable by construction. "Write the tests first" was also the only discipline with no home in the document; every other one (verification, context, documentation, falsification, cleanup) is a section with checkboxes, while test-first lived as a column value, so the *deviation* had machinery and the *rule* did not. **Test Phase 1 is mandatory, first, and the register**: it authors every test that can honestly be authored and records every one that cannot, via `#### Deferred to Test Phase N` (required for any later test phase, refused by name without it), `#### Deferred to Build Phase N` (one test authored late, body optionally carried as a fenced block so the justification is checkable rather than promised), and `#### Regression Guards` (a row passing in the phase that authors it has no red window — legitimate for a guard, identical in shape to a rubber stamp, and nothing can separate them mechanically, so the author declares which). The register **absorbs `### Trajectory Rationale`** — one home per fact. **Gate A corrected**: `<=` on the document timeline rather than `===`, so a row left unwritten blocks every later checkoff instead of being asked about once and never again, and a test phase's own Verification gate also advances the obligation. **Zero migration** — `### Phase N` still means build phase N, and the 52 existing impls validate untouched (asserted over the real corpus, not a fixture). Also corrects `/work`'s `.skip()` advice against a fact executed rather than assumed: a file whose import cannot resolve fails to load **even when every test in it is skipped**, so `.skip()` is not a deferral mechanism — it is right only when the symbol exists and the behaviour does not.

### Removed
- **The last of jj, and the audit that could not see it** (`jj-residue-rip-out`). `git-only-substrate` declared jj removed in 1.31.0 and its enforcement test has been green ever since — for a structural reason, not a factual one. That plan's assertion scoped the search to `apps/indusk-mcp/src/` and to five TypeScript *identifiers*, matched **one line at a time**. `apps/indusk-admin/src/lib/vcs.ts` fell outside all three: it lives in the other app, calls jj as an argv string, and formats `execFileSync(` and `"jj",` on separate lines. It had been running `jj log` **first, per commit id, on every scorecard render** for seven weeks, with a doc comment asserting jj was "the project default in dusk." The file predates the rip-out by two months, so this was residue the sweep never looked at rather than a reintroduction. Removed: the jj execution path, four pieces of UI copy telling users to run `jj describe` (the eval hook has fired on `git commit` only since 1.31.0, so following those instructions produced nothing), a `jjDescription` prop the audit's own pattern could not match (`/\bjj\b/` needs a word boundary), the `scm?: "jj" | "git"` config field and its `indusk update` nudge — a deprecation opened at 1.31.0 and closed here at 1.36.1 — plus stale comments and one docs page. **Deliberately preserved**: the superseded `git-or-jj-substrate` decision and lesson pages, ~60 archived planning files, `guide/scm.md`, the changelog's own historical entries, and the three bundled community lessons that use jj as their worked example. Those are evidence; evidence with the specifics removed stops teaching, and an audit that fires on the archive gets switched off — which is how the first one ended up unable to fail. The replacement audit scans **both apps**, matches jj at **argv level against whole-file content**, exempts the record explicitly, and — the actual deliverable — **was observed red before any removal**, naming all three violations including the one seven weeks of green had hidden.

  Falsification then caught the replacement making the *same* mistake in a third dimension. The predecessor was blind on **path** and **pattern**; this plan fixed both and left **file type** open, scanning `.ts`/`.tsx` only. That is not academic: `guide/getting-started.md` — the first page a new user reads — advertised **`/jj`** among its available skills, and `skills/jj.md` was deleted in 1.31.0. A dead instruction sat on the front door for seven weeks, the rip-out shipped past it, and the new guard was structurally incapable of seeing it, because a guide is prose. The audit now covers the surfaces that actually ship to consumers — hooks (`.js`), skills (`.md`), extension manifests (`.json`) and docs — with a **separate pattern set for prose**: jj as an *instruction* (`jj <subcommand>`, `` `/jj` ``) rather than the bare token, because `\bjj\b` also matches the `-jj-` in legitimate cross-references to `git-or-jj-substrate`, the very page being preserved. Widening to prose immediately pulled the changelog, strategy and dawn pages into reach, so the preserved set had to be encoded rather than merely decided in prose — it had been ruled preserved earlier in the plan and recorded nowhere a machine could read. One more defect fell out of the same pass: the matcher called `exec` on module-level regexes, so a pattern carrying `/g` would have leaked `lastIndex` between files and silently under-reported — proven with a two-call test that returned one match and then none, then fixed.

### Fixed
- **Hooks now load in consumers that declare `"type": "commonjs"`** — which, since npm 11, is every project created by `npm init -y`. The hooks are ESM; node tolerates that in a `.js` file only when the nearest `package.json` is *silent* about `type` (it retries the parse as a module and warns). State `"type": "commonjs"` outright and node refuses with `SyntaxError: Cannot use import statement outside a module` — so **every hook died at load and no gate fired**, silently, in any newly-created project. Nothing in InDusk was CommonJS; npm changed the default and older versions had simply omitted the field. It went unnoticed because this repository is in the one configuration that masks it — dusk's own root `package.json` has no `type` field, so the repo dogfooding the hooks is exactly where they still work. `init` and `update` now write a nested `.claude/hooks/package.json` declaring `{"type":"module"}` (module type resolves to the *nearest* package.json, so one file scopes it to that directory — cheaper and safer than renaming ten hooks to `.mjs`, which would rewrite every `settings.json` registration and need a consumer-side migration path that does not exist yet). **Running `indusk update` repairs an existing broken project**, verified end to end against a real 1.35.1 consumer. Present in 1.35.1 and earlier.

## [1.36.0] — 2026-08-05

### Added
- **`indusk run <plan> --model <name>` (`atdawn run`) — a model-agnostic gated execution loop** (`dawn-external-orchestrator`, Dawn Component 1). InDusk's discipline lived inside Claude Code; this decouples it. The brief's finding, verified before a line was written, is what made it cheap: **the gate scripts were already externalizable** — pure functions of *(edit intent + repo state) → allow/block* over a stdin/exit-code contract — so the orchestrator **reuses them as-is** through a ~50-line adapter rather than reimplementing a single rule. The agentic loop is rented from the Vercel AI SDK (v7), the only TS-native framework first-class across `@ai-sdk/anthropic`/`openai`/`google`/`xai` with genuine one-line provider swaps. InDusk owns only the thin shell: the adapter plus the loop control ported from `/work --autopilot` — scoped-per-phase, advance-on-green via a deliberate `check-gates` probe, goalpost guard, pause-at-human-gate. **The gate is enforced below the provider swap**, in the edit tool's own `execute` (spawn the gate program, write the tool call to stdin, apply on `0`, refuse with the block message on `2`), so gate behavior is structural rather than per-model; the SDK-native `toolApproval` callback sits above it as defense in depth, HMAC-signed so a forged approval is rejected before execution. Providers are hit **directly with your own keys** behind a ~20-line registry — no commercial gateway — which preserves per-provider credit arbitrage a middleman would bill away. Acceptance grew from a planned 4 cells to **9** across three models, two harnesses and two environments, and closed with a mutation-based quality read: **kill-rate 8/8 for every suite** — three models produced *detection-equivalent* tests, the deltas were style. No cell shipped a complete CLI, because the fixture's trajectory named only the pure functions and **models converge coverage exactly onto named acceptance** — an argument for naming the artifact you want in the trajectory, not for a better model. Routing conclusion: mechanical single-phase work goes to flash at ~2 orders of magnitude lower cost, quality-equivalent when the trajectory is well-named; sonnet buys unprompted test-first ordering, runnable artifacts and diagnostics.
- **Plan hierarchy in the admin UI — parents declare their children, top-down** (`dawn-ui-plan-grouping`, Dawn Component 0). The root `master.md` declares `parents:` and the `roadmap:` order; each parent's own `master.md` declares its ordered `subplans:`; **children declare nothing**, so each parent→child link has exactly one source of truth and the two sides cannot drift. **The plan inventory always comes from disk** (`parseAllPlans`) — declarations add structure and can never subtract a plan, and a missing, corrupt or absent key degrades to the flat list rather than hiding work. Machine truth moved into frontmatter because prose-derived structure **fails quietly**: the previous `readMasterPlanOrder` scraped markdown links by regex and silently matched *nothing* against the Dawn master, because those links carried a `../` prefix the pattern didn't expect. Declaration names are boundary values — segment-guarded (no `/`, `\`, `..`) and first-occurrence-deduped before any path join — and archived children resolve as real items with active winning a name collision, identically in the sidebar and the detail view.
- **`atdawn verify <plan> --phase N` — phase-boundary verification for work Dawn didn't execute** (`dawn-verify`, Component 6 of the Dawn master plan — the keystone). Every other enforcement in InDusk lives in the write path, which does nothing for a phase done in Cursor, a hookless session, or by hand: the edits are already applied and the boxes already checked. `verify` reconstructs the boundary from git and reports five detections — **premature checkoff** and **skipped test-first duty** (the former reusing the existing `check-gates` probe; the latter applied to phase N *directly*, because the probe asks about N+1 and neutralizes its rows, leaving the verified phase's own duty invisible to it), **goalpost drift** (`checkGoalposts` against `git show <baseline>:impl.md`), **red tests**, and **phantom work** (an item checked off with no corresponding change). It **detects and reports only** — reverting belongs to a later component and inherits the constraint that `git reset` cannot un-write a working tree. Baselines come from a **chained ledger** (`.indusk/verify/ledger.jsonl`, `merge=union`) whose record is a byproduct of the previous verification rather than a pre-dispatch ceremony someone must remember; a rejecting run records nothing, and a corrupt ledger refuses loudly rather than degrading into bootstrap mode against a wrong baseline. **Red-test detection closes a gap wider than tier 3: nothing in InDusk had ever executed a test as part of gate enforcement**, so `passing` in a trajectory State cell was an unverified self-report in *every* lane. Attribution uses an optional trajectory **`Test` column naming files**, run through the project's own command with the exit code as the verdict — runner-agnostic on purpose, since parsing a runner's output would hardcode tool knowledge into core; a row with no reference is reported **unverified**, never as passed. Acceptance: a 6-cell matrix against a hookless `claude` session (hook files present but unregistered — the Cursor shape) caught **5 of 5 planted violation classes with no false positive on an honest control**.
- **Hook parity for the Dawn thin lane** (`dawn-hook-parity`, Component 2 of the Dawn master plan). `atdawn run` now enforces every InDusk *invariant* and feeds the eval→lessons rail, closing the gap where the control-group lane produced no history, no scorecards, and no lessons. Four moves: **`claude-md-budget.js` joined the gate chain** (three shared scripts now; the block message is the script's own, so both lanes refuse an over-budget CLAUDE.md write identically); **loop-owned per-item commits** (the loop, not the model, commits each checkoff — deterministic across drivers, with commit failures surfaced loudly but never gating, and non-git worktrees disabling the cadence loudly); **a durable pending-eval queue** (`.indusk/eval/pending.jsonl` — each commit queues a record, `eval-trigger.js --drain-pending` evaluates each exactly once from any `claude`-capable machine, ledger written *before* spawn so a crash is a gap rather than a double-eval; `/rail-check` owns the drain and `check_health` surfaces the backlog); and **headless `ask` = pause** (a proof-less gate skip now pauses with the question and the exact proof format instead of red-stopping, making `ask` the default policy in both lanes with `auto` an explicit opt-in). `gate-reminder.js` is **deliberately shed** — an advisory nudge is not an invariant, and an unattended loop would spend scarce steps on advice the boundary gates already enforce; recorded as the first entry of the invariant/procedure keep-shed audit. Also corrects the Dawn master's hook inventory (5 on disk, not 6 — `check-plan-order.js` was deleted in the context-beam cleanup).

## [1.35.0] — 2026-07-25

### Added
- **`/work --autopilot {plan}` — hands-off phase-loop with fresh context per phase.** Multi-phase plans no longer need the manual per-phase `/handoff` → new session → `/catchup` → `/work` dance: autopilot executes each remaining phase in a **fresh subagent** (clean context, discarded after — only a compact result returns), advancing only on green. Its safety is structural, not trust-based: a spike this release **verified empirically that subagent tool calls fire — and are blocked by — this project's PreToolUse gate hooks**, so an autopilot subagent physically cannot skip test-first-RED or check off a phase whose trajectory rows aren't green. Human-judgment gates are **derived** from the plan's own Deferred Verification / manual-smoke / visual rows (no new marker); the loop hard-stops at impl-complete and hands back for `/falsify` — it never auto-runs the close-out rituals. A goalpost guard forbids autopilot subagents from editing the trajectory table or test assertions and snapshots-and-verifies the trajectory across each phase, so a gate can't be gamed. Additive — interactive `/work` is unchanged. Captured as the `community-subagents-inherit-pretooluse-hooks` lesson (a reusable harness fact for any subagent-orchestration feature).

## [1.34.0] — 2026-07-24

### Added
- **`/compact-context` skill — the bulk compaction ritual the budget hook has been promising.** The `claude-md-budget.js` error message told agents to "run the compaction ritual," but compaction shipped only as an incremental step inside `/retrospective` (demote one plan's narrative at close) — nothing could pay down a CLAUDE.md already multiples over budget, so the hook blocked *every* project's Key-Decision line once the file was over (numero: 497 KB / ~125k tokens/session). The new skill classifies every entry (load-bearing convention / shipped narrative / operational state / dead), reports what it would demote and where each pointer targets (report mode is the default — `--apply` executes only on confirmation), creates any missing pointer-home pages, moves operational state to `.indusk/current.md`, and lands the file under budget in a single write with `indusk context check-pointers` verifying no dangling pointers. Complements — does not replace — the retrospective's incremental step. Hook error message now names the runnable command. Same defect class as the retired check-catchup hook (tooling demanding an action it didn't provide).

## [1.33.5] — 2026-07-24

### Removed
- **`indusk infra` CLI is gone.** The command managed the retired FalkorDB+Graphiti container and its output invited resurrecting it ("run `indusk infra start`") — observed steering an agent back toward the dead infrastructure. Stop/remove any remaining container with plain docker commands.

### Fixed
- **rail-check skill summary no longer renders as `"|"`** — its multiline frontmatter description defeated the `get_skill_summaries` parser; collapsed to one line.

## [1.33.4] — 2026-07-24

### Fixed
- **Removed the permanently-unsatisfiable `check-catchup.js` hook** (found by the versioned-workbench POC): fresh projects deadlocked — init wrote an unchecked handoff.md Catchup Status AND registered the hook demanding it be checked, whose manual path probed the retired FalkorDB/Graphiti. `init` no longer ships the hook or the handoff scaffold; `update` deletes the installed hook and strips its settings registration on every project.

## [1.33.3] — 2026-07-23

The first complete 1.33.x release: indusk-makeover + posthog extension together. (1.33.0 and 1.33.2 were accidental pre-merge trunk publishes — deprecated; 1.33.1 has the makeover but not posthog.)

## [1.33.2] — 2026-07-23 (deprecated — pre-merge trunk publish)

### Added
- **`posthog` extension — PostHog product analytics via the official remote MCP server.** Backported from numero's third-party extension into the built-in catalog. Manifest wires `mcp.posthog.com` into `.mcp.json` with Bearer auth from `.indusk/extensions/posthog/.env` (`POSTHOG_MCP_URL` + `POSTHOG_MCP_API_KEY` — a `phx_` **personal** API key, not the `phc_` project ingestion token; the key's project access is the visibility boundary). Ships `.env.example` + an agent-facing `skill.md` (query events/persons/insights/replays/flags/experiments/errors; read-side only — app event capture stays a runtime concern). Health checks: `posthog-mcp-configured` + `posthog-token-present`. Opt-in via `indusk extensions enable posthog`; auto-detected when a `posthog` entry already exists in `.mcp.json`. Catalog entries under a new "Product analytics" group in both extension indexes — PostHog is the what-users-do layer, complementary to the `dash0`/`datadog`/`local-telemetry` what-services-do layer.

(Also a version-alignment republish of 1.33.1's makeover content, plus the merge to main.)

## [1.33.1] — 2026-07-23

(1.33.0 was an accidental publish of 1.32.0-era trunk code and is deprecated on npm — 1.33.1 is the real indusk-makeover release. Content below.)

### Changed (InDusk Makeover — budgets + decay + removal)
- **CLAUDE.md hard budget (60 KB) enforced at write time.** New `claude-md-budget.js` PreToolUse hook blocks over-budget Edit/Write on any file named CLAUDE.md and warns at 90%; budget configurable via `context.claude_md_budget_bytes`. Discipline-only compression was tried and failed — the hook is the ratchet. New `indusk context check-pointers` verifies every path reference in CLAUDE.md resolves (under rule+pointer compression, a dead pointer is a lost rule body). See [the context-budget guide](/guide/context-budget).
- **Decay layer.** `indusk agent sweep` archives current.md sections older than `agents.sweep_ttl_minutes` (default 7 days — distinct from the 60-minute display TTL) to `.indusk/archive/current-md-archive.md`; `indusk plans archive-dead` moves all-draft stale plans (older than `planning.dead_draft_days`, default 30) to `planning/archive/`. Archive, never delete. See [`plans` CLI reference](/reference/cli/plans).

### Fixed (Phase 7 falsification)
- **Budget hook predicts Edits with literal splice, not `String.replace`** — `$`-substitution patterns in a replacement string (plausible in shell/regex snippets) made the size prediction diverge from what the Edit tool actually writes, in either direction. Empty `old_string` is guarded.
- **Legacy-server removal is a shared, tested migration helper** (`lib/mcp-migration.ts`) — init and update call one implementation; future MCP retirements extend its list.
- **`list_plans { active: true }` includes `completed`** — a completed impl still in `planning/` is awaiting close-out rituals; hiding it from the dieted catchup hid exactly the plans mid-close.

### Removed (Graphiti + CGC)
- **Graphiti and CodeGraphContext are retired.** MCP registrations, extensions (`graphiti`/`cgc`/`falkordb`), the semantic-graph event pipeline, the context beam, and `indusk graph`/`indusk beam` CLI are all removed. Field measurement: ~1 generic Graphiti query per session, near-zero CGC reads, standing infra + schema cost. `indusk init`/`update` now REMOVE stale registrations and disable the retired extension manifests on existing projects.
- **The indusk-infra container (FalkorDB + Graphiti) is retired.** `indusk init` no longer checks or auto-starts it. Stop yours with `docker stop indusk-infra`; the `indusk-data` volume is retained until you remove it manually (`docker volume rm indusk-data` once you're sure nothing in the graph is still wanted).
- **The highlight→eval rail is preserved and retargets to lessons.** The eval agent materializes durable highlights via `add_lesson` (titles-hot/bodies-cold); findings persist via the eval scorecard/findings log. The CRITICAL live-delta and `already_processed` STOP guards from 1.31.x carry over unchanged — a reappearing `graph_capture` reference is now itself a test failure.

## [1.32.0] — 2026-07-13

### Added (Worktree Visibility — worktree-per-plan, observable in the bulletin)
- **`indusk agent list` now shows each session's worktree + branch, recomputed live.** `agent register` records the session's worktree toplevel and branch; `agent list` gains `WORKTREE`/`BRANCH` columns and **recomputes** both from cwd in its self-heartbeat (not the register-time snapshot), so the who-is-where board never drifts as an agent moves between the trunk and a worktree. The `**Branch**:` / `**Worktree**:` markers join `sanitizeSectionBody`'s forbidden-marker list so a section body can't spoof them.
- **Same-tree collision flag.** When two or more live sessions resolve to the same worktree toplevel — the real case being two agents both editing the shared trunk — `agent list` prints a `⚠ collision:` warning to stderr, and `/catchup` surfaces it. This is the exact concurrent-session collision class that worktree-per-plan is designed to prevent, now made observable.
- **Worktree-per-plan is the default at impl kickoff.** `/work`'s new Worktree Kickoff step reads the impl frontmatter via `resolveWorktreeDecision` and, for a plan sitting in the trunk, **nudges** to `indusk worktree create {plan-slug}` before any code is written (`detectTreeContext` classifies trunk vs worktree). One plan → one branch → one worktree → PR → merge-and-delete. Opt out per-plan with `worktree: none` in the impl frontmatter — **no workflow ships it by default** (this revises `planner-hotfix-mode`'s "no worktree" decision; hotfix now gets a worktree too). Nudge, not a hard gate, in v1 — the collision flag makes an ignored nudge self-announcing. New `lib/worktree/decision.ts`. See the [multi-agent guide](/guide/multi-agent) and [decision](/decisions/worktree-visibility). Plan: `worktree-visibility`.

### Added (Cleanup Ritual — plan-close decomposition)
- **`/cleanup {plan}` — the plan-close decomposition ritual, the structural twin of `/falsify`.** Runs AFTER falsification, before `/retrospective` (`/work → /falsify → /work → /cleanup → /work → /retrospective`). It investigates the plan's changed files, applies the **enabled domain extensions'** best practices (nextjs: split `use client` islands / server-client boundaries; react: one component per file) for what-to-extract, and **authors a `### Phase N: Cleanup` phase** into `impl.md` with extraction/refactor recommendations that `/work` then executes. The ritual authors; it never refactors inline. Motivated by numero's accretion failure (a 1,439-LOC `page.tsx` with zero decomposed components despite a documented ≤200-LOC convention) — advisory conventions don't hold; enforced, reviewable rituals do.
- **No mechanical LOC gate — the threshold is attention-focus only.** A new `cleanup` config block in `.indusk/config.json` (`max_file_loc`, default 400, + per-scope `{ include, max_file_loc?, test_sibling? }` overrides) tells the ritual which changed files to *scrutinize*, never blocks. `getCleanupConfig` / `resolveCapForPath` readers in `lib/config.ts` (defaults in the reader; absence → default). `init` scaffolds the block; `update` migrates existing projects idempotently via `ensureCleanupConfig`. Over-extraction is a failure mode: the ritual recommends only what best practices warrant, and "leave as-is" with a recorded reason is a first-class outcome.
- **`/retrospective` Step 0 now requires BOTH rituals.** Renamed "Ritual Gate — Falsification + Cleanup", it hard-blocks unless a terminal Cleanup Phase exists (`isCleanupComplete`) OR `cleanup: skipped` + `cleanup_reason` is present (`isCleanupSkipped`). `checkRetrospectiveReadiness(planRoot, implContent)` composes both. Skill-level enforcement mirroring the falsification gate — no fifth per-phase gate type, no hook changes. New `lib/cleanup/oversized.ts` (`listOversizedChangedFiles`, merge-base diff) + `lib/cleanup/gate.ts`.
- **Trajectory validator now accepts `A`-prefixed test IDs** alongside `T` (`/\b[TA]\d+\b/`) in the cross-reference and rationale-completeness checks — acceptance-style IDs from a test plan no longer need remapping to `T`. Bounded to `[TA]` (not `[A-Z]`) so `H`/`P` refs don't false-match; TS source + JS hook port changed in lockstep with a parity test. See the [Cleanup Ritual guide](/guide/cleanup-ritual) and [decision](/decisions/cleanup-ritual). Plan: `cleanup-ritual`.
- **Falsification hardening (H1/H2/H4/H8)** — the ritual's `/falsify` pass on itself found four real bugs, all fixed in scope: the retrospective gate now detects ritual phases by a title that **starts with** "Cleanup"/"Falsification" (not a substring, so a phase like "The /cleanup skill" isn't misdetected as the ritual phase); `checkRetrospectiveReadiness` honors the default phase-authored falsification flow (a terminal Falsification phase, no legacy log); `ensureCleanupConfig` keys on cleanup-block presence so a user block with only `scopes` isn't clobbered on `indusk update`; and `listOversizedChangedFiles` resolves the merge base through candidate bases (`origin/main` → `main` → `master` → root) so it works on a local repo with no fetched `origin`.

### Added (`indusk setup` — one-shot workbench creation)
- **`indusk setup <cloned-repo-path>` turns an already-cloned git repo into a workbench in one command** — collapses the four-step manual Flow A (mkdir workbench, hand-write `package.json`, symlink the trunk, `init --workbench --wrapped-repo X --sibling-parent Y`) into a single verb and removes the `--sibling-parent` footgun. Derives `<repo>-workbench` name + parent from the path, scaffolds the workbench `package.json`, creates a non-destructive **symlink-in-place** trunk (`<repo>-workbench/<repo> -> ../<repo>` — the clone is not moved, a dirty tree is fine), then delegates to `init --workbench`. Errors (exit 1) on a non-git/missing path or when `<repo>-workbench` already exists (pointing at `indusk update`); all guards run before any `mkdir`, so a failed setup creates nothing. New `apps/indusk-mcp/src/bin/commands/setup.ts` + `cli.ts` registration; 7 subprocess integration tests in `setup-command.test.ts` (T1–T7). Reference: [`indusk setup`](/reference/cli/setup); guide: [Worktree setup — Flow A](/guide/worktree-setup). Plan: `workbench-setup-command`.

## [1.31.12] — 2026-06-30

### Fixed (eval→Graphiti rail — hook↔evaluator arg-name contract break)
- **`eval-trigger.js` passed the state root as `statePath`, but the evaluator reads `projectRoot` — `undefined` crashed the eval agent before any work, silently killing the rail (third bug in the 1.31.7/1.31.10 rail-integrity lineage)** — surfaced 2026-06-30 by Sandy tracing the dawn-fde-toolkit rail: 11 unprocessed highlights back to 2026-05-07, `results.log` showing the evaluator failing with `The "path" argument must be of type string. Received undefined`. **Root cause**: 1.31.7's workbench-aware rewrite taught the hook to compute `statePath` (where `.indusk/` lives) and `gitPath` (the git repo) and changed the inline spawn script to pass `{ statePath, … }` to the evaluator. But both entry points — `runPersistentEval` (`persistent-evaluator.ts`) and `runEvaluatorSync` (`evaluator-runner.ts`) — still destructure `opts.projectRoot`. So `projectRoot` was `undefined` and the evaluator's first statement, `initEvalOtel(opts.projectRoot)` → `isEvalOtelEnabled` → `join(undefined, …)`, threw `ERR_INVALID_ARG_TYPE` before building a prompt or spawning `claude`. With `stdio: "ignore"` + the inline `.catch`, the crash logged to `results.log` but never surfaced — no scorecard, no highlight to Graphiti, on every project (single-repo and workbench alike). The earlier "Could not find @infinitedusky/indusk-mcp package" error was a *prior, different* failure; resolving the global install to 1.31.11 uncovered this next one. **Fix**: the hook now passes `projectRoot` (the key the evaluator reads) plus a new `gitRoot` carrying the git repo. `EvaluatorRunOptions` and `runPersistentEval`'s opts gain `gitRoot?: string`; all three `claude` spawn cwds (`runEvaluatorBackground`, `runEvaluatorSyncInner`, `runPersistentEval`) use `opts.gitRoot ?? opts.projectRoot` so the inner `git show ${changeId}` runs in the repo even when the state root is a non-git workbench root. Single-repo callers and the baseline CLI omit `gitRoot` and fall back to `projectRoot` unchanged. **Why the tests missed it**: every existing eval test calls the entry points directly with the correct `projectRoot` key; the hook's emitted arg shape was never asserted against the signature. New `apps/indusk-mcp/src/__tests__/eval-trigger-evaluator-arg-contract.test.ts` (6 cases) pins the contract from both sides — the hook must pass `projectRoot` (never `statePath`) + `gitRoot`, only keys the evaluator accepts; and both entry points must declare `gitRoot?` and use the `gitRoot ?? projectRoot` spawn-cwd fallback. Reproduced live before/after: old `statePath` shape crashes at `initEvalOtel`; new shape sails past it to a normal (stubbed) spawn.

### Cross-references
- Same rail, same lineage as [1.31.7] (workbench path split) and [1.31.10] (CJS-require-in-ESM + gitPath fallback). 1.31.7 introduced the `statePath`/`gitPath` concepts in the hook but renamed the evaluator arg without updating the evaluator signature — this patch closes that gap.
- Lesson reinforced: when a JS hook port and its TS callee share an argument contract, a source-grep test must assert the *emitted keys match the accepted keys* — direct-call unit tests on the callee can't catch a caller-side rename.

## [1.31.11] — 2026-06-29

### Added (context-budget — Pieces 1 + 2 of brief)
- **`indusk prune --dry-run` measurement surface** — new CLI command that audits InDusk context bloat without modifying anything. Reports CLAUDE.md size by section (flagging sections > `--large-section-chars`, default 4000 chars), per-lesson ages (flagging lessons untouched > `--stale-lesson-days`, default 180), per-current-md-section ages (flagging sections older than `--stale-section-days`, default 7), total estimated auto-loaded bytes per catchup, and recommended manual cleanup commands. **`--dry-run` is the default and only mode in v1** — no `--apply`, no destructive action. Auto-pruning is intentionally deferred to Piece 3 of the context-budget plan (the larger architectural shape: current.md auto-archive + `context.budget_tokens` config field + beam-default catchup). Library lives at `apps/indusk-mcp/src/lib/prune/measure.ts` as a pure-read function consumable by future tooling (admin UI, retrospective skill, etc.) without going through the CLI. 8 vitest cases across `prune-measure.test.ts` (T1-T5: library shape, degraded mode without `.indusk/`, threshold-flagging for CLAUDE.md / lessons / current.md) and `prune-cli.test.ts` (T6-T8: CLI invocation prints + no-file-modification + --help disclaimer + bloated-CLAUDE.md surfacing).

### Changed (retrospective skill — one-line Current State entries)
- **`/retrospective` skill Step 7 (Context Audit) now documents the one-line + link-to-archive shape for Current State entries** — replaces the previous implicit pattern where retrospectives wrote multi-paragraph entries that accreted in CLAUDE.md indefinitely. Over 20-30 plans the paragraph pattern pushes CLAUDE.md past 30KB of always-loaded prose, paid by every Claude Code session forever. New shape: `- **{plan-name} ({version})** — one-sentence summary. See [archive](.indusk/planning/archive/{plan-name}/) for full detail.` The skill now includes a concrete example showing the right shape AND a counter-example block warning AGAINST multi-paragraph entries with the rationale (token bloat on every catchup, the detail lives in the archive anyway). Existing multi-paragraph entries unchanged — operator collapses via `indusk prune --dry-run` surface + manual cleanup. 5 source-grep cases in `retrospective-skill-one-line.test.ts` (T9-T10) defend the discipline against future skill drift: assert Step 7 contains the one-line phrasing, includes a concrete example, has a counter-example warning, names the token-cost rationale, references the context-budget plan.

### Notes
- The immediate token-reduction impact on Numero comes from the operator's MANUAL one-time CLAUDE.md diet (~30 min by hand) collapsing existing multi-paragraph Current State entries to one-liners. Pieces 1 + 2 make the diet sustainable going forward but don't perform it.
- Piece 3 (current.md auto-archive + `context.budget_tokens` config field + beam-default catchup) is its own subsequent plan with full ADR. See [context-budget brief](https://github.com/infinitedusky/dusk/blob/main/.indusk/planning/context-budget/brief.md) for the architectural design.

## [1.31.10] — 2026-06-28

### Fixed (workbench-mode-rail-integrity — Falsification Phase, two bugs)
- **`persistent-evaluator.ts:68` no longer uses CJS `require()` in an ESM module — and the regression scan now covers every file under `src/lib/eval/`** — surfaced by Sandy tracing the Numero rail mid-drain on 2026-06-28. The runner spawned from `eval-trigger.js` prefers `runPersistentEval` when `persistent-evaluator.js` is installed (`useFunction = existsSync(persistentEvaluatorPath) ? "runPersistentEval" : "runEvaluatorSync"` at `eval-trigger.js:297`). `persistent-evaluator.ts` is a `type: "module"` ESM file but had `const { unlinkSync } = require("node:fs") as typeof import("node:fs");` at line 68 inside `clearSession()`. Node throws `ReferenceError: require is not defined in ES module scope` when that line executes, crashing the eval agent silently. Sandy bypassed this for the manual backfill drain by invoking `runEvaluatorSync` directly. **Fix**: replaced the inline `require()` with a top-of-file ESM `import { unlinkSync } from "node:fs"`. **Root-cause discipline gap**: the existing regression test at `apps/indusk-mcp/src/__tests__/falsification-hook-esm-require.test.ts` scanned only `apps/indusk-mcp/hooks/eval-trigger.js` — same bug pattern, different file, no coverage. Broadened the test to recursively walk `apps/indusk-mcp/src/lib/eval/**` against the existing CJS-require regex; any future require() of `fs` or `path` in any sibling module now fails this test with file:line offender listing. Same pattern as `community-anchor-shell-trigger-patterns-no-substring` — when a regression test exists, it should cover the bug CLASS (every file at risk), not just the specific file that bit you the first time.
- **Workbench-mode gitPath fallback when `event.cwd` is the workbench root** — 1.31.7's path-resolution fix was incomplete. `findGitPath` derived gitPath via `git rev-parse --show-toplevel` against `event.cwd`. Claude Code's PostToolUse hook event carries the SESSION cwd (where Claude Code was launched), NOT the subprocess cwd that ran `git commit`. When the session is launched from the workbench root (which IS the dominant operating model on Numero and future FDE engagements), `event.cwd` is a directory that's not a git repo — `git rev-parse` fails — the hook bails silently every commit. The 1.31.7 fix only worked when `event.cwd` happened to be inside a git repo, which is the rare case. **Fix**: new `findGitPathFromWorkbenchConfig(statePath)` in `apps/indusk-mcp/hooks/_hook-paths.js`. When `findGitPathFromCwd` returns null, the fallback reads `${statePath}/.indusk/config.json`, looks for `worktree.wrapped_repo`, and returns `realpath(${statePath}/${wrapped_repo})` if that path exists. Otherwise null (caller logs an actionable skip naming `statePath`). **Trade-off accepted**: if the user committed in a SIBLING WORKTREE rather than the wrapped repo, this gives the eval agent the wrapped repo's HEAD instead of the worktree's. Known incompleteness — a future patch could read `tool_input.command` for a `cd <worktree>` prefix. Until then, fallback is strictly better than the pre-1.31.10 silent bail. 4 new vitest cases in `hook-paths.test.ts` under `T2b: workbench-mode gitPath fallback`: (a) `event.cwd` = workbench root → gitPath resolves from config, (b) no `wrapped_repo` in config + no git repo at cwd → null gracefully, (c) cwd IS inside a git repo + config has wrapped_repo → `git rev-parse` wins (existing behavior preserved), (d) config names a nonexistent path → null.

### Cross-references
- Falsification Phase appended to `.indusk/planning/workbench-mode-rail-integrity/impl.md` with hypothesis rows H1/H2 + fix items pinning the regression tests in scope.
- 1.31.7's brief and U1 outcome both depended on `event.cwd` happening to be a git repo. The 1.31.10 falsification demonstrates that assumption was wrong for the dominant Numero use case (`pnpm wt`, top-level commits, Claude Code launched from workbench root). Reframing the U1 success criteria so that "drain succeeds via the auto-rail" instead of "drain succeeds via manual `runEvaluatorSync`" is the right success bar — Sandy's manual drain via `runEvaluatorSync` proves the eval agent CAN process the queue; 1.31.10 + restart proves the rail fires it AUTOMATICALLY on the next commit.

## [1.31.9] — 2026-06-28

### Fixed (worktree extension — wt.sh raw-exec via `--` separator)
- **`pnpm wt <slug> -- <binary> [args...]` runs the binary directly without prepending `pnpm`** — surfaced by Sandy running `pnpm wt solana-migration docker compose --env-file .env.local up -d --force-recreate poker` and hitting `node: .env.local: not found`. Root cause: `wt.sh` always prepends `pnpm` to whatever follows the slug (lines 132 and 137 — `exec pnpm "${COMMAND[@]}"`). For pnpm-script targets (`pnpm dev`, `pnpm build`) and pnpm-aware tools (`pnpm ce`), that's correct. For raw external binaries like `docker`, the prefix means pnpm 9+'s built-in `--env-file` flag consumes the user's `--env-file .env.local` for its own env loading, garbles the downstream args, and lands as `node: .env.local: not found` somewhere in the exec chain. **Fix**: support `--` as a separator. With `--` as the first command token, `wt.sh` exec's the remaining args directly — no pnpm prefix, no flag interpretation. Sandy's command becomes `pnpm wt solana-migration -- docker compose --env-file .env.local up -d --force-recreate poker` and works as expected. Backwards-compatible — existing `pnpm wt cancel-polish dev` continues to invoke `pnpm dev` in the worktree. 4 new vitest cases in `apps/indusk-mcp/src/__tests__/worktree-wt-resolve.test.ts` cover (a) the raw-exec path with a binary in PATH (`echo`) carries args through verbatim including `--env-file`, (b) `--` with no following command errors with a clear usage message, (c) `:<app>` + `--` runs raw exec inside `apps/<app>/`, (d) absence of `--` still uses the pnpm prefix. Header line also distinguishes the two modes: `[alpha] echo raw-args` (raw) vs `[alpha] pnpm dev` (pnpm-prefix).

## [1.31.8] — 2026-06-28

### Added (workbench-mode-rail-integrity — Phase 5 prep: post-update agent skill)
- **New `/rail-check` skill** — canonical post-update verification procedure invokable by trigger phrase. Skill description literally enumerates the phrases that should trigger it: *"I just updated indusk"*, *"post-update"*, *"rail check"*, *"backfill"*, *"verify the rail"*, *"the eval pipeline"* — so the working agent's `/catchup`'s `get_skill_summaries` step surfaces the skill in its output and the agent recognizes conversational triggers without slash-command invocation. The skill walks a 6-step procedure for both workbench and single-repo projects: detect project shape → run `check_health` → walk through stray-state findings with per-directory confirmation (never auto-delete) → sanity-commit smoke test verifying `evaluator spawned` appears in `system.log` → count unprocessed highlights → drain the queue via a single `node .claude/hooks/eval-trigger.js --source rail-check-backfill` invocation that drains all unprocessed highlights in one pass → verify Graphiti gained episodes via `mcp__graphiti__get_episodes`. "When NOT to invoke" section ensures the skill doesn't interrupt in-flight work. Companion at `apps/indusk-mcp/test-fixtures/numero-workbench-shape/README.md` documents the same procedure for manual operator use (failure-modes table included). **Sandy's use case**: after `npm i -g @infinitedusky/indusk-mcp@1.31.8 && indusk update`, he says to the Numero agent *"I just updated to 1.31.8"* and the agent runs the procedure end-to-end, confirming each `rm -rf` and each backfill. No manual orchestration past the four-word prompt. Skill ships in `apps/indusk-mcp/skills/rail-check.md` and gets auto-installed to consumer projects as `.claude/skills/rail-check/SKILL.md` via the existing `globSync("*.md")` skill-sync in `init.ts`/`update.ts`.

## [1.31.7] — 2026-06-28

### Fixed (workbench-mode-rail-integrity — the 2-month dark-queue rail fix)
- **Workbench-aware path resolution in all 4 hooks — eval→Graphiti pipeline now works on workbench-shaped projects** — surfaced 2026-06-28 by tracing the `numero_workbench` pipeline and finding 86 unprocessed highlights / 0 Graphiti episodes across 2 months. **Root cause**: the 4 hooks (`eval-trigger.js`, `check-catchup.js`, `check-gates.js`, `validate-impl-structure.js`) each carried their own copy of `findProjectRoot()` that walked up looking for `.indusk/`. In workbench-shaped projects (per the `worktree` extension's shape: `.indusk/` lives at the workbench root, wrapped repo lives in a subdirectory) that landed at the workbench root — which is NOT a git repo. `eval-trigger.js` then ran `git rev-parse --short HEAD` against the workbench root and bailed silently with `skip — no git commit ID available`. The eval agent never spawned. The dark queue accumulated for 2 months. **Fix**: new shared helper `apps/indusk-mcp/hooks/_hook-paths.js` exports `resolveStateAndGitPaths(cwd)` returning `{statePath, gitPath}`. `statePath` walks up from `cwd` looking for `.indusk/` OR `.claude/` (either marker counts). `gitPath` runs `git rev-parse --show-toplevel` against `cwd` — in workbench mode this is the wrapped repo (or a worktree); in single-repo mode it equals `statePath`. The two may differ. All 4 hooks refactored to consume the helper. `eval-trigger.js` specifically: `git rev-parse --short HEAD` now runs with `cwd: gitPath` (the load-bearing fix); the spawned eval agent's `cwd` is `gitPath ?? statePath` so the inner `claude --print` inherits a directory where `git show ${changeId}` works. **Discovered bug fixed in scope**: early `syslog` calls (lines 56/70/82/97 pre-refactor) passed raw `cwd` to syslog — in workbench mode that wrote to `wrappedRepo/.indusk/eval/system.log` instead of `workbenchRoot/.indusk/eval/system.log`, silently creating stray `.indusk/` directories inside the wrapped repo. Moved `statePath` resolution earlier so all syslogs write to the correct location. New tests: `hook-paths.test.ts` (T1/T2/T3 — single-repo, workbench, worktree cases), `eval-trigger-workbench-mode.test.ts` (T4 — load-bearing workbench-mode end-to-end, T5 — single-repo regression), `hooks-workbench-refactor.test.ts` (T6/T7 — source-grep + behavioral guards on the 3 remaining hooks).
- **Stray-state audit in `check_health` for workbench-shaped projects** — Sandy's "no lingering app-level state" requirement. New module `apps/indusk-mcp/src/lib/stray-state-audit.ts` exports `findStrayState(workbenchRoot)` returning `StrayStateFinding[]`. Detects workbench mode via `.indusk/config.json`'s `worktree.wrapped_repo` field. In workbench mode, walks down 2 levels inside the explicit wrapped repo path looking for stray `.indusk/` directories (artifacts of pre-1.31.7 `indusk init` runs or operators running init from inside the wrapped repo — these silently confuse path resolution). Worktrees at workbench-root level (siblings to the wrapped repo) may legitimately have their own `.indusk/` for per-worktree scratch and are NOT flagged. Returns `[]` in single-repo mode (no false positives). Each finding has `{path, type, recommendation}` where `recommendation` is a literal `rm -rf "<path>"` command. **`check_health` wired**: each finding becomes an error check named `workbench/stray-state-<type>`; the tool response gains a top-level `stray_state` array. **Never auto-deletes** — the `/rail-check` skill (1.31.8) walks the operator through per-directory confirmation. New tests: `stray-state-detection.test.ts` (T8/T9/T10/T11 — stray-detected, clean-workbench, single-repo-skip, worktree-not-flagged).
- **Retroactive falsification finding for `indusk-worktree-extension`** — the worktree extension's brief asserted that workbench-shape worked end-to-end without grep-verifying that all 4 hooks understood the new layout. Textbook `community-brief-author-bias-ground-truth-verification` failure (the lesson published in 1.31.4). When `indusk-worktree-extension`'s still-owed falsification + retrospective run, this 1.31.7 fix is the rail-integrity gap the extension's original shipping missed. Cited explicitly in `.indusk/planning/workbench-mode-rail-integrity/brief.md` "Notes for next session" so the retrospective inherits the finding automatically.

### Deferred Verification
- **U1: Real numero_workbench backfill drain** — 86 highlights → Graphiti episodes. Requires actual `claude --print` execution against the live `numero_workbench` `.indusk/highlights.jsonl` after 1.31.8 is installed via `indusk update`. Cannot be programmatically asserted in a unit test (depends on real Claude behavior + Graphiti container state + the actual 86 entries' content). Mitigation: the new `/rail-check` skill (1.31.8) automates the operator-facing procedure; the manual fallback is documented at `apps/indusk-mcp/test-fixtures/numero-workbench-shape/README.md`. Sandy runs after publish; if the drain produces ≥80 episodes and `system.log` shows `evaluator spawned` for the sanity commit, U1 flips to passing. If it fails, Phase 2 (the eval-trigger refactor) reopens.

## [1.31.6] — 2026-06-28

### Fixed (list_plans malformed-frontmatter tolerance)
- **`mcp__indusk__list_plans` tolerates malformed plan frontmatter — single bad YAML doesn't poison the entire listing** — surfaced when one Numero plan's frontmatter contained an unquoted-colon `title:` field (the `migrate to Solana as soon as: feasible` shape). `gray-matter`'s `matter()` threw on the YAML parse, propagated up through `parsePlan` → `parseAllPlans` → the MCP tool, and broke plan tooling for every plan in the project. Same shape as the admin-UI registry quarantine pattern from 1.27.5: malformed input gets isolated, not contagious. **Fix**: `parseFrontmatter` in `apps/indusk-mcp/src/lib/plan-parser.ts` wraps `matter(raw)` in try/catch and on YAML parse failure writes a `[plan-parser] frontmatter parse error in <file>: <message>` line to stderr and returns `{ frontmatter: null, parseError: { file, message } }` instead of throwing. `determineStage` propagates `parseError` up; returns `stage: "malformed"`, `stageStatus: "parse-error"` when frontmatter failed. `determineNextStep` emits an actionable next step: `Fix YAML frontmatter at <file>: <error>`. `parseAllPlans` adds defense-in-depth: wraps `parsePlan` in try/catch so any unexpected throw (filesystem race, etc.) produces a placeholder `PlanSummary` rather than poisoning the listing. The broken Numero plan now appears in `list_plans` output with `stage: "malformed"` + `nextStep` naming exactly what to fix; every other plan appears normally. 2 new vitest regression cases in `apps/indusk-mcp/src/lib/plan-parser.test.ts` cover (a) unquoted-colon frontmatter produces a malformed PlanSummary without throwing and (b) one malformed plan does not poison `parseAllPlans` — other plans still appear with correct stages.

## [1.31.5] — 2026-06-28

### Changed (catchup performance — lazy-load lessons)
- **`mcp__indusk__list_lessons` returns titles + paths only, NOT full content — ~5-15× context-cost reduction per catchup** — surfaced when Sandy noticed catchup taking a long time and asked whether lessons should be moved to Graphiti. They shouldn't (lessons are markdown-narrative-shaped, not entity-shaped; moving them breaks `add_lesson` discipline and graceful degradation), but the catchup-load problem was real: pre-1.31.5, `list_lessons` returned the full markdown content of every lesson on every call (~30 lessons × ~3KB = ~90KB per catchup). **Fix is at the TOOL level**: `list_lessons` now returns `{file, path, type, title}` per lesson — no `content` field. The catchup skill's Step 4 is rewritten from *"Read every lesson"* to *"Skim every lesson's title + description; the title is the actionable rule in most cases. Read full content via the `Read` tool against the returned `path` only when a lesson is relevant to the work the user is about to do."* Lessons stay as markdown files (narrative preserved, graceful-degrade preserved, `add_lesson` discipline preserved, git history of evolution preserved). Most catchups will now load ~6KB of titles + 0-3 selective full reads ≈ ~10-20KB total — a 5-15× reduction from the status-quo ~90KB. Tool's description string updated with a `note` field instructing agents to use the `Read` tool on `path` when a lesson's title matches the work. Verified: only consumers of `list_lessons` are catchup (updated) + init.ts permission allowlist (just names the tool, doesn't read content). Safe additive removal.

## [1.31.4] — 2026-06-28

### Added (cross-project agent discipline — community lessons + boundary callout)
- **3 personal lessons promoted to community + new lesson on the highlight-not-direct-Graphiti boundary** — Sandy asked whether other InDusk projects (Numero, future engagements) would naturally know about the three-tier agent-roles discipline. Answer: yes, structurally — community lessons land in every project's `.claude/lessons/` on `indusk update` and the catchup skill explicitly instructs the agent to *"Read every lesson. These are rules learned from past mistakes — not suggestions. Internalize them before touching any code."* So the right primitive to ship cross-project rules is the community-lessons directory. **Promoted from dusk-personal**: `community-brief-author-bias-ground-truth-verification` (every "X already works" claim in a brief needs source-code annotation before acceptance — Phase 6 H1 of `git-or-jj-substrate` cost ~4 hours because 3 brief claims about `eval-trigger.js` were wrong; verification at brief time would have been ~20 minutes; ~12× cost ratio). `community-anchor-shell-trigger-patterns-no-substring` (never use `String.includes` for shell-command trigger detection — H3 of Phase 7 found `git committer` substring false-positive; use word-boundary or right-edge-lookahead anchored regex). `community-graceful-degrade-architecture-trap` (substrate decisions don't graceful-degrade safely — the dual-SCM model paid 6 weeks of compounding maintenance before git-only-substrate ripped it out; pick a substrate or solve the "we don't know yet" problem first). **NEW**: `community-use-highlight-not-direct-graphiti-writes` distills the three-tier discipline into a single agent-facing rule: working agent writes via `mcp__indusk__highlight`; eval agent is the sole structured Graphiti writer at trigger points. Names what reads ARE allowed (`mcp__graphiti__search_*`/`get_*`), the trigger-point conventions per skill, common failure modes ("Graphiti is down — write the highlight anyway"), and how to grep yourself for violations.
- **Catchup skill Step 7 — explicit "Reads only" callout naming the boundary** — pre-1.31.4 catchup's Graphiti-recall section showed the read pattern but never named the write rule. New agents reading catchup cold could miss it. Step 7 now explicitly says *"Catchup queries Graphiti — it does not write to it. As a working agent, you never call `mcp__graphiti__add_memory` or `mcp__indusk__graph_capture` directly in process skills."* Points at `community-use-highlight-not-direct-graphiti-writes` for the full discipline. Together with the 4 community lessons above and the existing skill-level enforcement (in `highlight.md`, `planner.md`, `work.md`, `retrospective.md` all consistently name "the working agent does not write the Graphiti episode directly"), Numero's agent now sees the three-tier discipline by Step 4 of catchup, without `apps/docs/src/guide/agent-roles.md` (the human-facing version) reaching the agent.

## [1.31.3] — 2026-06-28

### Fixed (eval-agent-mcp-access Phase 7 — malformed-line bypass defense)
- **`markProcessed` defends against malformed historic entries (Phase 7 falsification fix)** — Phase 6's write-time dedup (shipped in 1.31.2) relied on `readAllProcessed`, which silently skips malformed JSON lines via try/catch around `JSON.parse`. If `highlights-processed.jsonl` contains a corrupted entry for the target ID (truncated line from a crash mid-write, a hand-edit gone wrong, a version-format mismatch), the dedup check thought the ID wasn't present and appended a duplicate — Phase 6's contract silently broke for this specific input. **Fix**: a new file-private `isIdInRawProcessed(projectRoot, id)` helper scans the raw file content for the literal `"id":"<escaped id>"` substring via a JSON-safe regex (defensive metacharacter escaping). `markProcessed` calls it after the parsed-content check returns no match: if the raw bytes carry the ID, return `{ already_processed: true, original_processedAt: undefined }` without appending. The boolean is the load-bearing signal for the agent's STOP path; `original_processedAt` is undefined because the malformed entry didn't yield a parseable timestamp. The parser's read-side tolerance is preserved — `readAllProcessed` still skips the malformed line; the defense works around it on the WRITE side without changing the parser. Regression: 2 new cases in `highlights.test.ts` covering the malformed-truncated-JSON case and confirming `readAllProcessed` tolerance is preserved. Surfaced by the `/falsify` ritual run on the freshly-shipped 1.31.2 Phase 6 code.

## [1.31.2] — 2026-06-28

### Fixed (eval-agent-mcp-access Phase 6 — highlights dedup)
- **`markProcessed` rejects duplicate IDs at write time (1.31.2)** — T4's runtime audit of the 1.31.1 fix surfaced a gap: across 4 successful post-upgrade evals on dusk, the eval agent called `mcp__indusk__highlights_unprocessed` in only 3 of them. The 4th eval processed highlights from session memory instead, producing 43 duplicate `graph_capture` writes for the same 52 unique highlight IDs (`highlights-processed.jsonl` grew to 98 lines for 52 unique IDs). The dedup logic in `readUnprocessedHighlights` was correct (uses a Set on processed IDs) but `markProcessed` was designed as idempotent-append — duplicate writes silently accepted, no signal back to the agent. **Fix**: two-pronged. (a) Strengthened the Step 4 prompt instruction in `buildHighlightsInstructions` with a **CRITICAL** preamble: *"You MUST call `mcp__indusk__highlights_unprocessed` first. Do NOT process highlights from session memory. Do NOT read `.indusk/highlights.jsonl` directly. ONLY process IDs returned by the tool."* Plus an explicit instruction for the `already_processed: true` response: *"STOP processing it immediately. Do NOT call `mcp__indusk__graph_capture`."* (b) Changed `markProcessed` from idempotent-append to write-time rejection: if the ID is already in the processed log, return `{ already_processed: true, original_processedAt }` without appending. The agent's tool result now signals the redundancy so downstream `graph_capture` calls don't happen for already-done IDs. Regression: 3 new vitest cases in `apps/indusk-mcp/src/lib/highlights/highlights.test.ts` covering the new contract; T8 source-grep in the eval-resume-prompt test file pins the "MUST call" + "don't process from memory" + "already_processed" instructions in the helper body.

## [1.31.1] — 2026-06-28

### Fixed
- **Eval agent now drains the highlights queue on every commit, not just first-spawn (1.31.1)** — the `eval-agent-mcp-access` plan's April 1.23.x fix added `--mcp-config .mcp.json` + `--permission-mode bypassPermissions` to the spawn args, restoring MCP tool access in the inner Claude subprocess. That fix was correct AT THE TIME but a second bug went unnoticed: the *resume* path in `persistent-evaluator.ts:222-245` hand-rolled a minimal "Evaluate a new commit ... output the JSON scorecard" prompt and silently omitted Step 4 (process unprocessed highlights). The fresh-spawn path goes through `buildEvaluatorPrompt` (which includes Step 4); the resume path was the lean cousin. Across 197 evals on the persistent session (created 2026-04-19, last used 2026-06-28), the inner Claude received Step 4 exactly once — the April fresh spawn — and processed 3 highlights. Every commit since has been a resume that received no Step 4 instruction; the highlights queue stopped draining and grew unbounded (52 unprocessed at fix time). **Fix**: extracted the Step 4 highlights instruction into `buildHighlightsInstructions({ projectGroup })` in `prompt-builder.ts` and have the resume prompt prepend it. Both paths now reach the same instructions. Regression: `apps/indusk-mcp/src/__tests__/eval-resume-prompt-includes-highlights.test.ts` source-greps both the helper export and the call site in the resume branch, pinning the load-bearing call order (highlights instructions BEFORE the commit-evaluation literal). T4 (live smoke against the persistent session) verifies the runtime drain.

### Fixed (1.31.1 falsification ritual — H14, H15, H16)
- **Resume prompt no longer anchors the inner Claude to "as before" (H14)** — the pre-fix wording said `"answer the same evaluation questions as before"`. Inside a 197-turn persistent session where Step 4 was never previously provided, "as before" reads as "your last turns" and pulls Claude back to the pre-Phase-4 pattern of skipping Step 4. Reworded to `"answer the v1 rubric questions for this commit"` — present-tense direct instruction, no backwards-anchoring temporal modifier. Trailing `"as before — no commentary"` also dropped.
- **`buildHighlightsInstructions` explicitly handles the empty-list case (H15)** — the pre-fix helper text only handled `highlights_unprocessed` being *unavailable* (tool not loaded). Once the backlog drains (or in fresh projects), the tool returns `[]` and the inner Claude has undefined behavior — could hallucinate highlights, loop searching, or skip smoothly. Added an explicit branch: `If the tool returns an empty list (no unprocessed highlights), note "(no unprocessed highlights)" once and continue to the rubric — do not invent highlights, do not loop searching for them, do not call \`graph_capture\` speculatively.`
- **Source-grep regression for the 1.23.x MCP-access flags (H16)** — T3 from Phase 4 pins the prompt shape but not the spawn flags. If someone refactors `persistent-evaluator.ts`'s `args` list and removes `--mcp-config` or `bypassPermissions`, the inner Claude has no MCP tool surface and Step 4 fires into a void — silent return to the April-2026 failure mode. New T7 assertion: both flag literals appear in source with count ≥ 2 each (locking the resume + fresh spawn-arg sites in place).

### Fixed (1.31.0 falsification ritual — H1, H3, H5)
- **`indusk init` warns when the project is not a git repository** (H1) — Phase 4 deleted the pre-1.31.0 deferred-SCM stderr warning; nothing was added in its place. `indusk init` in a non-git directory got a green-looking init and the user discovered git was required only when `indusk graph sync` crashed or the eval hook silently skipped. The warning is back, scoped to "git is the only SCM" with `git init` named as the recovery. Informational — init still exits 0.
- **`indusk graph sync` translates git-state errors to friendly messages** (H3) — `runSync()` calls `getCurrentChangeId()` which awaits `execFileAsync("git", ["rev-parse", "--short", "HEAD"])`. Previously, the rejection propagated through `cli.ts`'s action as an unhandled `Error` with the raw `ChildProcess` stack trace. Now the CLI wraps the call in try/catch and translates "not a git repository" → "run `git init` first" / "unknown revision" → "run `git commit --allow-empty -m 'init'` first". Exits with code 1. The same translation lives in `tools/graph-tools.ts`'s `graph_sync` MCP handler so MCP callers get the same UX.
- **Eval-trigger `TRIGGER_RE` no longer matches `git commit-tree` / `git commit-graph`** (H5) — pre-existing bug inherited from the dual-form `\b(jj describe|git commit)\b` regex and not tightened during the Phase 2 collapse. JS's `\b` matches the `t`→`-` transition (word-char to non-word-char), so `\bgit commit\b` matched git plumbing commands. Tightened to `/\bgit commit(?=$|\s|;|&|\|)/` — lookahead requires end-of-string, whitespace, or a shell separator after `commit`. The porcelain `git commit ...` still matches; plumbing commands no longer fire the eval hook. New CLAUDE.md gotcha records the constraint so future regex changes don't reintroduce the bug.

## [1.31.0] — 2026-06-27

### Changed
- **SCM substrate is now git-only (1.31.0)** — InDusk's dual-SCM era (1.28.x–1.30.x) ends. Git is the only supported source control system. Existing `scm: "jj"` config fields become a no-op; `indusk update` emits a one-line stderr nudge (`scm field no longer used; safe to remove from .indusk/config.json`) and leaves the file unchanged. See [git-only-substrate](/decisions/git-only-substrate) for the full decision; see [Git workflow guide](/guide/scm) for the convention.

### Added
- **Semantic graph populates on every git project (Phase 1)** — `indusk graph sync`, `captureWithLog`, and the MCP `graph_*` tools now produce real events on git projects. The prior graceful-degrade no-op is gone. Rebase tolerance comes via content-keyed dedup at sync time (`existingByIdentity` on path + `existingByFingerprint` on blob hash) — `git rebase` produces noisy-replay-then-converge: extra `anchor.*` events get written; the runtime de-duplicates by `(path, blob_hash)` identity on the next sync; the system converges to current file state after one cycle. Provenance traceability is fuzzy (an event's `change_id` may name a rewritten commit) but functional correctness holds. Three obsolete tests (`git-mode-graph-sync.test.ts`, `git-mode-graph-cli.test.ts`, `git-mode-e2e.test.ts`) deleted; replaced by `git-tmp-project-graph-sync.test.ts` (T1, T3, T4) and `graph-capture-git-mode.test.ts` (T2, T5).
- **Eval pipeline collapsed to single-SCM (Phase 2)** — `apps/indusk-mcp/hooks/eval-trigger.js`'s `TRIGGER_RE` narrowed from `/\b(jj describe|git commit)\b/` to `/\bgit commit\b/`; change-ID extraction collapsed to a single `git rev-parse --short HEAD` call (no jj-first-then-fallback). `prompt-builder.ts` no longer accepts an `scm` field; `diffCommand` is `git show ${changeId}` everywhere. `persistent-evaluator.ts` + `evaluator-runner.ts` no longer call `getScm()`. `indusk eval baseline --task` collapsed to `git commit --allow-empty` + `git rev-parse --short HEAD`. The TDZ-on-scm-const gotcha in `persistent-evaluator.ts` is no longer applicable.
- **Skills collapsed to git-only (Phase 3)** — `apps/indusk-mcp/skills/jj.md` deleted. `work.md`'s dual-form `### If scm: "jj"` / `### If scm: "git"` commit-cadence sections collapsed to single-form do-then-commit on a feature branch. `highlight.md`'s "next commit (jj describe / git commit)" prose collapsed to "next `git commit`". `eval-review.md`'s dual `jj diff` / `git diff` examples collapsed to git-only. `git.md` lost its "if your project uses Jujutsu" framing.

### Removed
- **SCM abstraction layer (Phase 4)** — `apps/indusk-mcp/src/lib/scm/detect.ts`, `apps/indusk-mcp/src/lib/semantic-graph/jj.ts`, `getScm()`, `detectScm()`, `NoScmDetectedError`, `getJjReachable`, `NotAJjRepoError`. `lib/scm/index.ts` shrank from ~85 lines to ~60 (single git impl). `lib/semantic-graph/jj.test.ts` + `scm/detect.test.ts` + `scm-init-detection.test.ts` + `init-deferred-scm-warning.test.ts` deleted. `apps/docs/src/guide/scm.md` rewritten as "Source Control: Git". The `git-or-jj-substrate` planning brief gained a supersession banner.

## [1.30.2] — 2026-06-27

### Fixed
- **eval-trigger hook now finds pnpm-global installs (1.30.2)** — pre-1.30.2, `apps/indusk-mcp/hooks/eval-trigger.js` checked two npm-style candidate paths (`<bin>/../lib/node_modules/...` and `<hook>/../../node_modules/...`) and gave up. Pnpm-global installs live under `<pnpm-root>/@infinitedusky/indusk-mcp/...` — neither candidate hit — so every `git commit` / `jj describe` on a pnpm-installed indusk-mcp logged `Could not find @infinitedusky/indusk-mcp package` to `.indusk/eval/results.log` and exited without spawning the evaluator. Highlights queued indefinitely; nothing landed in Graphiti. Dusk itself surfaced this after 22 hours and 44 unprocessed highlights. **Fix**: extended the candidate list with two additional lookups, both guarded by try/catch (so missing `pnpm` or `npm` on PATH is non-fatal): `pnpm root -g` joined with `@infinitedusky/indusk-mcp/dist/lib/eval/evaluator-runner.js`, and the same for `npm root -g`. The npm-root lookup also covers mise-managed Node installs and custom prefixes where the `<bin>/../lib/node_modules/...` shape isn't accurate. Backlog of queued highlights drains on the next commit after upgrade — no manual recovery needed. Regression: `apps/indusk-mcp/src/__tests__/eval-trigger-pnpm-lookup.test.ts` — source-level grep, same pattern as the existing eval-trigger hook tests.

## [1.30.1] — 2026-06-27

### Added
- **Body-content rules surfaced in handoff skill + MCP tool description (1.30.1)** — the body sanitization rules from 1.30.0 Phase 6 falsification (rejecting `^---\s*$`, `^##\s+Session\s+`, `^\*\*Session ID\*\*:`, `^\*\*Last updated\*\*:` at start of line + control characters in `sessionId`) were enforced at the lib boundary but not surfaced to agents. The handoff skill now carries a "Body-content rules" subsection listing the four forbidden patterns + the workaround (backtick or indent), and the `mcp__indusk__update_current_section` tool's Zod schema descriptions now spell the rejections out per-field. Agents in downstream projects (numero, concierge, etc.) no longer hit silent `TypeError` rejections with no warning. Closes the gap surfaced during the section-shape retrospective audit — skills auto-sync to consumers via `globSync("*.md")` in `update.ts`; the MCP tool description rides along with the tool registration.

## [1.30.0] — 2026-06-27

### Added
- **Multi-agent coordination (1.30.0)** — concurrent Claude Code sessions on one project no longer collide. The old singleton `.claude/handoff.md` model had two reliable failure modes (catchup-mid-write blocking the other agent; handoff overwriting the previous handoff); both are structurally impossible in the new shape. **Three primitives**: (a) worktrees per agent (substrate from F1, already shipped), (b) **per-agent sections inside a single `.indusk/current.md`** — `## Project (shared)` anchor any agent edits, plus `## Session <short> — <task>` blocks each session owns, (c) the **`mcp__indusk__update_current_section` MCP tool** as the explicit write surface for the agent's own section (atomic read-modify-write). `/catchup` is pure-read for shared content (only side effect is `agent register` + the self-heartbeat in `list`); `/handoff` is a four-step session-end ritual: call `mcp__indusk__update_current_section` with the three section bodies → commit → `indusk agent done` → fire eval-trigger. Session ID comes from `CLAUDE_CODE_SESSION_ID` (UUID, inherited by subprocesses); fallback is `pid-<process.pid>`. Stale TTL configurable via `agents.stale_ttl_minutes` in `.indusk/config.json` (default 60). New `indusk agent register/done/list/prune` CLI — all operate on sections via the new `apps/indusk-mcp/src/lib/agents/current-md.ts` lib (parseCurrentMd / upsertSection / removeSection / pruneStaleSections / listSections). `indusk init` writes the new template, gitignores `.indusk/agents/` as precaution (the directory is no longer used), writes `agents.stale_ttl_minutes: 60`, and sets `.indusk/current.md merge=union` in `.gitattributes` so two branches appending different sections merge cleanly. `indusk update` SHA-detects the empty original-shape template and migrates idempotently; user-edited current.md content is preserved untouched. **Cross-machine coordination is out of scope for v1** (laptop ↔ desktop bulletins are local-only; future plan). **Rejected**: lock-and-snapshot state machine (reintroduces mutation races; serializes work), in-repo bulletin committed to main (noisy commits + merge conflicts), distributed locks (overkill for single-machine), per-worktree presence (fails visibility), fixed-section current.md + separate presence files (no explicit write surface for the agent; required dropping the write-side gap mid-plan and reshaping). **Section-shape design note**: the merge story works because each agent only appends its own block. `merge=union` plus a parser that splits delimiter-bounded blocks on `## Session` boundaries handles git's automatic dedup of trailing `---` lines. New user-facing guide at `/guide/multi-agent`; CLI reference at `/reference/cli/agent`; MCP tool entry at `/reference/tools/indusk-mcp#agent-tools`; skill references at `/reference/skills/catchup` and `/reference/skills/handoff`; ADR at `/decisions/multi-agent-coordination`. **Side finding**: surfaced a stray-quote typo in dusk's `.indusk/config.json` (introduced in b31c1d60) that was silently making the OTel role validator default-on for the past month — fixed in scope. Plans: `.indusk/planning/handoff-multi-agent/` (original — superseded in part) + `.indusk/planning/handoff-multi-agent-section-shape/` (final shape).

- **Extensions index — catalog + decision matrix at `apps/indusk-mcp/extensions/README.md` and `/reference/extensions/` (1.28.13)** — with three observability extensions (dash0, datadog, local-telemetry), one project-management extension (asana), four code-intelligence extensions (cgc, graphiti, sigma, falkordb), one browser-automation extension (chrome-devtools), two diagram/docs extensions (excalidraw, vitepress), one emit-side extension (otel), and seven domain-skill extensions (docker, nextjs, react, solidity, tailwind, testing, typescript), the seam between extensions started showing — agents had no single page to consult when the question was "which extension do I use for X?" and the differentiation prose was scattered across 19 individual skill.md files. The index closes that gap with a decision matrix table mapping problems → extensions (e.g., "Query staging/prod logs from a Datadog account" → `datadog`; "Query staging/prod logs from a Dash0 account" → `dash0`; "Local-only OTel during dev" → `local-telemetry`) plus a grouped catalog organized by problem space (observability query/emit, project management, code intelligence, browser automation, diagrams, framework skills). **Cross-references added** to the observability cluster's `See Also` sections (dash0, datadog, asana, local-telemetry) so navigation works in both directions: from any individual skill back to the index, and from the index forward to the individual skills. **Internal vs user-facing**: `apps/indusk-mcp/extensions/README.md` is the package-internal version; `apps/indusk-docs/src/reference/extensions/index.md` is the user-facing version (linked from the docs sidebar under Reference). Both stay in sync — the closing "Adding a new extension" section explicitly tells contributors to update both. Sidebar updated to expose the index as `Reference → Skills → Extensions Index`.

- **Asana MCP extension (1.28.13)** — opt-in project-management extension wrapping [Asana's official V2 remote MCP server](https://developers.asana.com/docs/using-asanas-mcp-server) at `https://mcp.asana.com/v2/mcp`. Mirrors the dash0/datadog extension shape: `apps/indusk-mcp/extensions/asana/{manifest.json, skill.md, .env.example}`. **Auth**: OAuth 2.0 with PKCE on first session launch — same shape as datadog (no API token in env vars). Tokens expire after 1 hour, refresh tokens handle renewal automatically. **Permissions are bounded by the user's existing Asana access** — MCP tokens cannot do anything the authorizing user couldn't already do via the Asana web UI; all actions appear as that user in audit logs. **No regional variants** — Asana is single-region, so `.env.example` has just one URL line. **V1 endpoint deprecates 2026-05-11** (3 days from this ship); we bake in V2 only — the deprecated `https://mcp.asana.com/sse` is intentionally not offered as a fallback. **Distinct problem space** from the observability extensions (dash0, datadog): Asana is for work coordination — tasks, projects, comments, custom fields. Reach for it when the question is about WHO is doing WHAT, not about service health. **Pre-registered clients only** — Asana V2 doesn't support dynamic client registration; Claude Code is pre-registered, so users don't need to register a client_id themselves; OAuth just works. **Not required-by-default** — opt-in like dash0/datadog; projects that don't use Asana see no change. Health check verifies the MCP server is registered in `.mcp.json`.

- **Datadog MCP extension (1.28.13)** — opt-in observability extension wrapping [Datadog's official remote MCP server](https://docs.datadoghq.com/bits_ai/mcp_server/) (launched March 2026, 16+ core tools plus toolset filtering for APM, Logs, Metrics, Monitors, Dashboards, Security Signals, Error Tracking, Feature Flags, DBM, LLM Observability). Mirrors the dash0 extension shape: `apps/indusk-mcp/extensions/datadog/{manifest.json, skill.md, .env.example}`. **Auth-model difference vs dash0**: Datadog uses **OAuth on first session launch** rather than a static Bearer token — `.env` only carries `DATADOG_MCP_URL` (regional endpoint), no secrets; OAuth completes in-browser. Setup: `cp .env.example .env`, uncomment your region's URL (US1/US3/US5/EU1/AP1/US1-FED supported), run `indusk extensions enable datadog`, complete OAuth on next Claude Code session. Toolset filtering via the `?toolsets=...` query param — defaults to `all` since Claude Code does its own client-side tool filtering. **Read-side only** — same convention as dash0: this extension configures how Claude queries Datadog, not how your services emit telemetry to Datadog (service-emit lives in your project's env-management setup). **Not required-by-default** — opt-in, like dash0; projects that don't use Datadog see no change. **No corresponding CLI** — Datadog's `dd-cli` is for emit-side instrumentation and isn't wired by this extension. Health check verifies the MCP server is registered in `.mcp.json`.

### Fixed
- **FullscreenDiagram component — three silent-failure bugs in the docs viewer (1.28.13)** — found while authoring docs on dawn-fde-toolkit; the same bugs were latent in dusk and in the template that `indusk init-docs` ships to consumer projects. **Bug A — underscore-prefix breaks `<script setup>` template bindings.** Handlers were declared as `_toggleExpand`, `_zoomIn`, `_zoomOut` (likely auto-fixed by `noUnusedVariables` + `argsIgnorePattern: "^_"` because the linter can't see template references). The template binds to `@click="toggleExpand"` etc. without the prefix, so Vue resolved bindings to `undefined` — clicks became silent no-ops. Fix: rename handlers without the prefix; add a Biome override disabling `noUnusedVariables` for `**/*.vue` (linter can't parse `<template>` blocks; the cost of letting unused script-setup vars slip is far smaller than silently broken click handlers). **Bug B — `bounds: true` + `boundsPadding: 0.1` silently locked panning** when the rendered SVG roughly filled the modal — drag did nothing because the constraints left zero pannable slack. Fix: `bounds: false` (Figma/Miro convention is unbounded panning with a reset button). **Bug C — wheel-to-zoom default felt wrong for diagram viewers.** `panzoom`'s default treats wheel as zoom; users expect wheel-to-pan with Cmd/Ctrl-wheel-to-zoom (matching every modern canvas tool). Fix: pass a `beforeWheel(e)` callback that pans on plain wheel and lets panzoom's zoom proceed when `e.ctrlKey || e.metaKey`. **Files changed**: `apps/indusk-docs/src/.vitepress/components/FullscreenDiagram.vue` (the dusk docs site copy), `apps/indusk-mcp/templates/FullscreenDiagram.vue` (the template `init-docs` ships to consumer projects), `biome.json` (override added), `biome-rationale.md` (entry added documenting why the override exists). Both .vue files are now byte-equal copies of the same fixed source. **Convention** (not enforced, project-by-project): wrap every mermaid block in `<FullscreenDiagram>` with blank lines around the inner fenced block — VitePress requires the blank lines to parse the mermaid markdown inside the Vue component. Will become the default in the `init-docs` scaffold once the convention stabilizes.

### Added
- **`get_project_info` MCP tool + community-graphiti-group-id-underscores lesson — close the silent-empty Graphiti recall loop (1.28.13)** — surfaced 2026-05-07 on dawn-fde-toolkit: agent queried `mcp__graphiti__search_nodes({ group_ids: ["dawn-fde-toolkit"] })`, got empty results, concluded "graph empty." Two failure modes were stacked: (1) hyphens in `group_id` hit a RediSearch syntax error which the lib catches and returns empty; (2) falling back to no `group_ids` filter doesn't scan all groups — also returns empty. Both look identical to "graph has nothing." InDusk's `getProjectGroupId(projectRoot)` already sanitizes hyphen → underscore on writes, but the agent had no callable tool to learn the sanitized form on reads — the `/catchup` skill literally said "call `getProjectGroupId(projectRoot)`," which is unreachable from agent context. **What changes**: (1) New `mcp__indusk__get_project_info` tool returns runtime project metadata as JSON: `project_group` (sanitized form), `scm`, `planning_dir`, `otel_role`, `otel_gate_emits`, `eval_enabled`, `eval_model`, plus a `graphiti_group_recall_example` block showing the exact `group_ids: [project_group, "shared"]` shape to use. Single source of truth for project metadata the agent needs at runtime — consolidates fields previously requiring 4 different reads (`getProjectGroupId`, `getScm`, `shouldEmitOtelGate`, `getEvalModel`). (2) `/catchup` skill prose rewritten — agents now call `mcp__indusk__get_project_info` to learn `project_group`, then pass it explicitly to graphiti queries. Old text said "the project group comes from `getProjectGroupId(projectRoot)`" which the agent couldn't actually invoke. (3) New community lesson `community-graphiti-group-id-underscores.md` documents both failure modes (RediSearch syntax error on hyphens; silent empty on missing filter) with the recovery shape. Lessons sync to all projects via `indusk update`. (4) `mcp__indusk__get_project_info` added to `init.ts` catchup permission allowlist so fresh inits don't see a permission prompt; existing projects will see a one-time prompt (low friction). **Why this matters**: silent empty was the worst failure mode — looked identical to "Graphiti is just empty" and the agent moved on without realizing it had skipped the recall step entirely. The lesson + tool surface make the convention impossible to miss.

- **`eval.model` config field — switches the eval agent's first-call model; defaults to `"sonnet"` (1.28.13)** — empirical pricing analysis on dawn-fde-toolkit revealed that fresh first-call evals running on Opus cost ~$4–7 each (matches Opus pricing math: 73K cache_create × $18.75/M + 1.83M cache_read × $1.50/M ≈ $4.35), while resume calls drop to ~$0.80 each (matches Sonnet math, not Opus). Cause: the hardcoded `--model opus` arg was set on first-call only; resume calls don't re-pass `--model` and inherit Claude Code's machine default, which is Sonnet for current installs. The hidden 5× cost arrived only on the catchup-eval spike. **What changes**: new `eval.model` field in `.indusk/config.json` controls the model passed to `claude --print --model <arg>` on fresh first-call evals. **Default flips from `"opus"` (hardcoded) to `"sonnet"` (config-driven)** — matches what resume calls already do, cuts catchup cost ~5× ($4–$7 → ~$0.90 per fresh call). Set `eval.model: "opus"` to opt back into Opus on first-call. Accepts any value Claude Code's `--model` accepts: shortcuts (`opus`, `sonnet`, `haiku`) or full IDs (`claude-sonnet-4-6`). **Helper at `apps/indusk-mcp/src/lib/config.ts:getEvalModel(projectRoot)`** — single read site; both `evaluator-runner.ts` and `persistent-evaluator.ts` (no-session path) use it. **Caveat**: resume calls do NOT re-pass `--model` (Claude Code's `--resume` interaction with `--model` is opaque from our side; could break sessions). So `eval.model: "opus"` only forces Opus on fresh-call. To verify what model is actually serving resume calls, inspect `~/.claude/projects/<pkg>/<sessionId>.jsonl | jq '.message.model'`. 8 unit tests in `src/lib/__tests__/eval-model-config.test.ts`. **Why this matters beyond cost**: pre-1.28.11 systems silently used the hardcoded Opus default, hiding the catchup spike from anyone not running per-eval cost analysis. Now the model is config-readable, env-overridable, and visible.

- **Agent conduct prompt as foundation for all agents (1.28.9)** — every project scaffolded by `indusk init` now ships an `AGENTS.md` file at the repo root containing canonical conduct directives (lead with the strongest counterargument; no warmup validation like "great question"; explicit confidence levels; generate own estimates before reading the user's; don't capitulate to pushback without new evidence; state negative conclusions plainly; if you don't know, say so; questions are questions, not instructions). The bundled `apps/indusk-mcp/templates/CLAUDE.md` prepends `@AGENTS.md` to import the directives — Claude Code's import syntax, matching the convention `apps/indusk-admin/CLAUDE.md` already used. **Eval agent inherits the conduct for free** — `claude --print` runs with `cwd: projectRoot` so CLAUDE.md auto-loads and `@AGENTS.md` resolves; no `prompt-builder.ts` changes needed. Subagents inherit the same way. **Init**: `apps/indusk-mcp/src/bin/commands/init.ts:476` copies `AGENTS.md` alongside `CLAUDE.md` (never overwrites if either exists). **Update**: `apps/indusk-mcp/src/bin/commands/update.ts:354` ensures pre-1.28.9 projects pick up `AGENTS.md` on the next `indusk update` (idempotent — `current: AGENTS.md` if it already exists; `added: AGENTS.md` plus a one-line note about adding the `@AGENTS.md` import otherwise). Deliberately does NOT auto-edit user-owned `CLAUDE.md` — preserves the pre-existing "never modify user CLAUDE.md" invariant; the `@AGENTS.md` import is a manual one-line paste. **Why**: surfaced 2026-05-04 — the system had no shared epistemics floor across working agent / eval agent / subagents. Skills are task-scoped (planner.md, work.md, etc.); CLAUDE.md is project-scoped factual memory; nothing said "this is how every agent should reason and speak." Without it, agent voice drifts toward sycophancy ("great question"), capitulation under pushback, anchored estimates, and decorative "you are an expert in all domains" priming that costs tokens without moving accuracy. The conduct file is small (~15 lines, ~1KB) and the eight bullets distill what's load-bearing without the verbose-expert framing of the prompt the user originally proposed (rejected: "make answers as long and detailed as you possibly can" directly contradicts the tool-using harness; "world class expert" doesn't move 4.x-class models on benchmarks; "verify your work" is vague vs the existing structural verify gates). **Dogfood**: `AGENTS.md` added at the dusk repo root + `apps/indusk-mcp/`; `@AGENTS.md` prepended to their `CLAUDE.md` files; conduct directives prepended above the existing Next.js note in `apps/indusk-admin/AGENTS.md` (which already used the import pattern). Eight files in the ship commit + version bump; 459 indusk-mcp tests passing; type-check clean. **Aligned with [agents.md](https://agents.md)** — the cross-CLI standard for agent operating instructions; future tooling that respects the convention picks it up without configuration.

- **InDusk now functions on plain-git projects, not just jj (`git-or-jj-substrate` shipped 1.28.13)** — adoption blocker for git-only teams resolved. Seven phases (5 build + 2 falsification rounds):
  - **Phase 1 — SCM detection + config field**: `scm: "jj" | "git"` in `.indusk/config.json` is the runtime source of truth. `indusk init` populates it via `detectScm()` (jj first, falls back to git, defers gracefully when neither is initialized so init still works in bare tmpdirs); `indusk update` migrates pre-1.28.x projects on the next run. New `apps/indusk-mcp/src/lib/scm/` module is the single abstraction layer — `getScm(projectRoot)` reads the config field; do NOT re-detect per call. `getScm()` defaults to `"jj"` when the field is missing for backward-compat with pre-1.28.x scaffolds; `indusk update` populates it.
  - **Phase 2 — Semantic graph wiring + graceful-degrade**: `lib/scm/index.ts` exposes branched `getCurrentChangeId(projectRoot)` and `getReachableChangeIds(projectRoot)` (jj path delegates to existing `lib/semantic-graph/jj.ts`; git path uses `git rev-parse --short HEAD` + `git log --format=%h HEAD`). All 3 semantic-graph callers (`sync-engine`, `graphiti-log-wrapper`, `replay` re-exports) routed through `lib/scm`. Git mode graceful-degrades: `runSync()` early-returns with `git mode — semantic graph unavailable` stderr; `captureWithLog()` warns once per session and skips the event-log mirror (Graphiti writes still succeed). `NotAJjRepoError` removed from public surface — internal jj-mode detail now. **Full git parity is deliberately out of scope for v1** (would need stable `event_id` UUIDs independent of SCM and rebase-tolerant replay); queued as a follow-up plan.
  - **Phase 3 — Eval surface SCM-aware**: `PromptBuilderOptions.scm` field; `buildEvaluatorPrompt` branches the diff-fetch instruction (`jj diff -r ${id}` for jj, `git show ${id}` for git); every caller (`evaluator-runner.ts` x2, `persistent-evaluator.ts` x1) passes `scm: getScm(projectRoot)`. `indusk eval baseline --task` CLI branches at the commit + change-ID step: jj uses `jj new` + `jj describe`, git uses `git commit --allow-empty -m` + `git rev-parse --short HEAD`. `findings.ts` doc comment + `eval` "no results yet" message both SCM-agnostic. **Gotcha caught in dogfood**: in `persistent-evaluator.ts`, `scm`/`diffCommand` consts MUST be declared above the `try { withSpan(...) }` block — `buildArgsAndPrompt` is called from inside the span callback and reads `diffCommand` from its enclosing scope; declaring them after puts them in TDZ and surfaces as 5 evaluator-spans test failures (missing `eval.spawn_claude` span).
  - **Phase 4 — Skills SCM-aware**: New `apps/indusk-mcp/skills/git.md` skill describing trunk-based development with short-lived feature branches, frequent commits + pulls, `--force-with-lease` discipline, merge + delete fast — the conventional GitHub Flow / big-org pattern. Sibling to `jj.md`; both stay separately authoritative for their respective SCM. `jj.md` byte-equal pinned to `apps/indusk-mcp/src/__tests__/fixtures/jj-skill-pre-phase-4.md` (regression target). `work.md` commit-cadence section dual-form (jj describe-then-do vs git do-then-commit on a feature branch); `highlight.md` + `eval-review.md` prose updated. Skills auto-sync via existing `globSync("*.md")` in `init.ts` and `update.ts` — no plumbing changes needed. New user-facing guide page at `apps/indusk-docs/src/guide/scm.md`.
  - **Phase 5 — End-to-end smoke**: New e2e harness at `apps/indusk-mcp/src/__tests__/git-mode-e2e.test.ts` exercising `git init → indusk init → 2x graph sync (idempotent no-op) → update preserves field` against a tmpdir. T8 manual smoke procedure documented at `apps/indusk-mcp/test-fixtures/git-mode-manual-smoke.md` (eval hook firing on `git commit` inside Claude Code) — runs require driving Claude Code itself so verification is human-only; T8 marked `skipped` until first real run.
  - **Phase 6 — Falsification round 1 (eval-trigger jj-only despite plan claim)**: ritual surfaced that the brief's claim "eval-trigger.js already works on git" was wrong — line 67 filtered for `jj describe` only and line 120 read change-ID via `jj log` with no git fallback. Plus init's hardcoded `hookFiles` array missed `eval-trigger.js` entirely (settings.json registered it but the file was never copied). Fixed: trigger filter accepts both `jj describe` and `git commit`; change-ID extraction tries jj first, falls back to `git rev-parse --short HEAD`; `init.ts` switched to `globSync("*.js")` matching update's pattern. Plus `cli.ts` `graph status`/`graph rebuild` and the matching MCP tools (`graph_sync`/`graph_rebuild`/`graph_status`) all early-return with `git mode — semantic graph unavailable` on git mode (instead of misleading `(no log file — run 'indusk graph sync' first)` hints).
  - **Phase 7 — Falsification round 2 (Phase 6's new code)**: ritual on the freshly-shipped Phase 6 code surfaced three concrete issues. **H3 substring false-positive**: `command.includes("git commit")` matches anywhere in the string, including `git config user.email "git committer"` (substring of "committer"), `cat git-commit-template.md`, `echo "git commit"` inside string literals — silent eval-runner spawns + junk scorecards. Fixed: filter is now a word-boundary regex `/\b(jj describe|git commit)\b/`. **H4 failed-commit noise**: PostToolUse hooks fire regardless of the underlying Bash command's exit code — a failed `git commit` (no staged changes, hook rejection, signing failure) would trigger eval against the previous commit's SHA, producing a misleading scorecard. Fixed: hook reads `event.tool_response.exit_code` and skips with `skip — bash command failed (exit_code=N)` when non-zero, before the trigger filter runs. **H5 init footgun**: Phase 1's deferred-SCM message was an inline parenthetical on stdout (`mode: full, scm: deferred — ...`) — easy to miss when scrolling init output. Fixed: separate stderr `⚠ WARNING` block at end of init when scm is deferred, naming `git init`/`jj git init` + `indusk update` as the recovery.
  - **Trajectory**: 18 rows. T1–T7, T9–T18 all `passing`. T8 `skipped` pending the first dawn-fde-toolkit manual smoke run.
  - **Test count**: 484 indusk-mcp tests passing (1 skipped). 21 new automated tests across init detection, graph sync graceful-degrade, lib/scm helpers, eval prompt SCM-branching, eval baseline source-level branches, skill prose, e2e harness, falsification phase tests (eval-trigger filter shape, exit_code skip, deferred-SCM warning).
  - **Discovered fix-in-scope items folded into the plan**: (a) init must tolerate `NoScmDetectedError` so it works in bare tmpdirs before `git init`/`jj git init` (Phase 1); (b) heavy subprocess tests need `{ timeout: 60000 }` on their describe blocks to survive parallel test pressure (Phase 3); (c) the persistent-evaluator TDZ trap (Phase 3 — `scm`/`diffCommand` consts must hoist above `try { withSpan(...) }` because `buildArgsAndPrompt` reads them from the span callback); (d) `init.ts` hardcoded `hookFiles` missed `eval-trigger.js` entirely — switched to globSync (Phase 6).
  - **Retrospective + T8 manual smoke**: pending. T8 awaits Sandy's first run on dawn-fde-toolkit (procedure at `apps/indusk-mcp/test-fixtures/git-mode-manual-smoke.md`).

### Changed
- **`indusk init` is now opinionated about Biome — adds devDep, migrates simple eslint/prettier/next-lint scripts, surfaces vestiges (1.28.8)** — pre-1.28.8 init wrote `biome.json` to the project root and stopped there. This produced exactly the half-done state we kept seeing in consumer projects: Biome is "configured" but `@biomejs/biome` is never installed, ESLint configs and lint scripts remain functional, Prettier still owns `format`. The user thinks Biome is the lint tool; the toolchain disagrees. Worse, `init.ts:868` actively respected detected ESLint via `detected.linter ?? "biome"`, recording `linter: "eslint"` in `.indusk/config.json` when it found `.eslintrc*` or `eslint.config.*` — so InDusk endorsed the existing tool rather than nudging toward the single-tool default. **What changes**: (1) `linterTool` is now hardcoded to `"biome"` in `writeConfig`; the `detected.linter` field is dropped from the config's `detected` block. ESLint detection becomes a *warning trigger*, not a preference. (2) New `wireBiomeIntoProject(projectRoot)` helper walks the root + `apps/*/package.json` + `packages/*/package.json` files and: adds `@biomejs/biome ^2.4.8` to root devDependencies if missing; replaces `lint` scripts that are SIMPLE invocations of `eslint` / `npx eslint` / `next lint` with `biome check`; replaces `format` scripts that are SIMPLE invocations of `prettier` / `npx prettier` with `biome format --write`. **Conservative on compound scripts**: if the script contains `&&` / `||` / `;` (e.g. `eslint src/ && tsc --noEmit`), it is left ALONE and surfaced in a `⚠ compound scripts skipped (manual migration needed)` block — auto-rewriting would silently drop the chained step. (3) New `detectLintVestiges` produces a `[Lint migration warnings]` block listing residual ESLint/Prettier config files (root + per-package) and residual `eslint*`/`prettier*`/`@typescript-eslint/*`/`eslint-plugin-*`/`eslint-config-*`/`prettier-plugin-*`/`@prettier/*` devDeps so the user can finish the cutover. **InDusk does NOT auto-remove user-visible files or devDeps** — that remains a manual step (a future `indusk migrate-to-biome` command may automate it; for now, the warning is the migration plan). **Why now**: surfaced 2026-05-01 on Numero — agent authored an impl item "Add ESLint rule enforcing import directionality" without checking the actual lint stack. Audit revealed `biome.json` configured but `@biomejs/biome` never installed, ESLint+Prettier vestiges across the monorepo. With Dawn's config-driven preference model not yet shipped, the right move is opinionated: single tool by default, the user opts out by ignoring the warnings or hand-editing `.indusk/config.json`. **Idempotent**: `indusk init --force` on an already-Biome-clean project is a no-op (skip line: "Biome already wired"). **Why Biome and not eslint**: single config (`biome.json`), single CLI (`@biomejs/biome`), single CI step. ~10–100× faster than ESLint+Prettier on typical workloads. ESLint+Prettier requires two configs + a third coexistence package (`eslint-config-prettier`); Biome's recommended ruleset is one coherent surface.

- **CodeGraphContext (CGC) removed from required catchup gate, eval allowed-tools, and beam pipeline (1.28.7)** — CGC moves from "required-by-default substrate" to "optional opt-in" in v1, foreshadowing the v2/Dawn pivot where CGC becomes one optional petal among many. **Catchup**: `/catchup`'s Step 0 no longer requires `mcp__codegraphcontext__list_indexed_repositories`; only the indusk MCP server is gated. The "Check Code Graph" step (formerly Step 7) is removed entirely. The `- [ ] graph` checkbox is dropped from the catchup status template (`apps/indusk-mcp/skills/handoff.md` + the init-scaffolded handoff in `apps/indusk-mcp/src/bin/commands/init.ts`). Health-check infrastructure verification still runs but no longer blocks on CGC. **Eval agent**: `mcp__codegraphcontext__*` removed from `allowedTools` in both `evaluator-runner.ts` and `persistent-evaluator.ts` — the spawned eval subprocess no longer has CGC tool access. **Beam pipeline**: `cgcRelationships` query (function-level callers/callees from CGC's structural index) removed from `BEAM_PIPELINE`; the source file `apps/indusk-mcp/src/lib/beam/queries/cgc-relationships.ts` deleted; `"cgc"` removed from the `BeamItem.source` and `QueryStep.source` union types in `beam/types.ts`; `format.ts` and `runner.ts` no longer reference cgc-specific filters or partition logic. **Extension intact**: the `cgc` extension itself, its skill (`apps/indusk-mcp/extensions/cgc/skill.md`), its health checks, and its MCP tools remain enabled and callable on demand — anyone wanting CGC for a specific blast-radius or dead-code query can still invoke `mcp__codegraphcontext__*` directly. The change is scope reduction, not removal: CGC stops being required infrastructure but remains available as an opt-in petal. **Why now**: CGC indexing was adding maintenance friction (catchup blocks when CGC was down, beam queries that returned noisy / low-signal results, the "never run bare cgc" Lite-vs-shared-FalkorDB trap) without proportional benefit. Foreshadows the Dawn ledger's D2 ("CGC as required → optional petal").

### Fixed
- **`indusk telemetry restart` preserves bound ports across restart (1.28.7)** — `daemonRestart` previously called `daemonStart(opts)` with whatever port options the caller supplied, which for the bare `indusk telemetry restart` invocation defaulted to `4318`/`16686` from commander. If the daemon had originally been started with `--otlp-port 0 --ui-port 0` (OS-assigned ports — the test convention, and a common dev pattern when avoiding collisions with a pre-existing global daemon), restart would silently switch to the hardcoded defaults, then fail to bind because the global machine-wide daemon already held those ports. **Fix**: `daemonRestart` now reads the previously-running daemon's `telemetry.json` meta file BEFORE stopping, captures `otlpPort` + `uiPort`, and forwards them to `daemonStart` unless the caller explicitly overrode via flag. CLI side: `telemetryRestart`'s options shape changed to `{ otlpPort?: string; uiPort?: string }`; the cli's `restart` action checks `Command.getOptionValueSource("otlpPort") === "cli"` to distinguish "user passed --otlp-port" from "commander supplied default", forwarding only the former. Restart now means "same daemon, fresh processes" — the contract T5 has always asserted but only happened to satisfy when the global daemon wasn't up.

- **`isPortFree` binds wildcard scope so it correctly detects collisions with `*:port` IPv6 wildcards (1.28.7)** — the helper previously bound to `127.0.0.1` (IPv4 loopback only) when probing for a free port. If another process held the port via an IPv6 wildcard (`::`) bind without `IPV6_V6ONLY` (which is what `net.createServer` does by default and what Jaeger does internally), `isPortFree` would return `true` for the IPv4-loopback-bind-succeeded check, then `findFreePort` would hand back the busy port to the caller, then Jaeger would fail at spawn time with `bind: address already in use`. **Fix**: bind without specifying a host, so the probe tests the same wildcard scope Jaeger and otelcol use. This change unblocked 6 tests across `telemetry-extension-{enable,disable}`, `telemetry-init-fresh`, and `telemetry-existing-project-upgrade` — all of which auto-start the daemon via `register` and were failing 100% on machines where the global daemon was up. In production, anyone running `indusk telemetry register` (the extension's on_enable hook) on a host with a pre-existing wildcard-bind collision would have hit the same false-positive collision detection.

- **Falsification integration test assertions aligned with the 1.27.4 phase-authoring contract (1.27.4 cleanup, shipped 1.28.7)** — `apps/indusk-mcp/src/lib/falsification/integration.test.ts` continued asserting that the `/falsify` skill markdown contained pre-rewrite strings (`appendHypothesis`, `markTerminated`, `Fix in scope` / `Spawn a new plan` / `Accept as finding` slugs, `## Three outcomes` heading) — left over from the sidecar-log regime that the 1.27.4 rewrite explicitly retired. The five failing assertions are updated to test the current contract: phase-authoring as the output, the `Falsification Phase` shape, the in-scope-vs-out-of-scope binary, and the `impl.md` (not log) target. No skill changes — the contract is correct; the tests were stale.

- **Plan-parser fixture references current active plans (1.28.7)** — `parseAllPlans` test in `src/lib/plan-parser.test.ts` previously asserted `agent-roles` and `dusk-v2` exist in the active planning dir. `agent-roles` was archived; `dusk-v2` was renamed to `indusk-v2-dawn`. Updated to `indusk-v2-dawn` + `react-native-support` (both parked, both unlikely to be archived in the near term). Pure fixture drift; no parser change.

### Fixed
- **Concrete version ranges for platform packages (1.28.2)** — 1.28.1's published `package.json` still had `"workspace:*"` as the version for all 4 `optionalDependencies`. `workspace:*` is a pnpm-only protocol; npm can't resolve it and fails during the dep graph walk (silent `exit 1` after manifest fetches in the npm debug log). pnpm is supposed to rewrite it to a concrete version at publish time, but the rewrite didn't fire (probably published via `npm publish` instead of `pnpm publish`, or the workspace-protocol-translation hook wasn't invoked). Fix: pin the 4 optional deps to `^1.28.0` in source, bump to 1.28.2, republish. The concrete-range pin doesn't change dev behavior — pnpm workspace resolution still uses the local package for in-repo consumers.

- **Republish indusk-mcp so optionalDependencies resolve (1.28.1)** — 1.28.0 was published before the four `@infinitedusky/telemetry-binaries-{darwin-arm64,darwin-x64,linux-arm64,linux-x64}` packages existed on npm. Consumers running `npm i -g @infinitedusky/indusk-mcp@1.28.0` hit `E404` during manifest resolution because npm treats even optional deps' 404s as hard errors when they appear in the dep graph. Fix: publish all 4 platform packages first, then bump indusk-mcp to 1.28.1 so fresh installs resolve cleanly. No code changes — this is a pure republish. If you installed 1.28.0 and hit the error, install 1.28.1 instead.

### Added
- **Local telemetry — new required-by-default extension + machine-global daemon (1.28.0)** — the first Arc 1 capability after admin-ui-hosting: every InDusk project now gets a localhost OTel backend for free. A new `local-telemetry` extension (required-by-default on every `indusk init` / `indusk update`) runs a machine-global daemon — Jaeger v2.17.0 + OTel Collector core 0.150.1 as native binaries, not containers — that absorbs dev-profile traces and logs and exposes them to both the agent (via MCP) and you (via `indusk telemetry *` CLI). Staging/prod OTel continues to route to Dash0 unchanged; only the `development` profile redirects to `http://localhost:4318`. **Daemon lifecycle**: `indusk telemetry start | stop | restart | status` — parallel shape to `indusk ui *`; metadata at `~/.indusk/telemetry.{pid,json,log}`, registry at `~/.indusk/telemetry/projects.json`. Both PIDs (Jaeger + otelcol) tracked and identity-verified via `verifyIdentity(pid, port)` (PID liveness + port listening) — same pattern that hardened the admin-UI daemon in 1.27.5. **Binary distribution**: four platform-specific npm packages at `@infinitedusky/telemetry-binaries-{darwin-arm64,darwin-x64,linux-arm64,linux-x64}`, listed as `optionalDependencies` on indusk-mcp with matching `os`/`cpu` filters — npm installs exactly one per consumer machine (esbuild/swc/biome pattern, zero custom download code). Build script at `scripts/build-telemetry-binaries.sh` fetches + SHA-verifies + packs per-platform; pins in `packages/telemetry-binaries-shared/UPSTREAM.json`. **Headline spike finding**: Jaeger v2 IS an OTel Collector distribution (ships `otlp` receiver, `batch`/`memory_limiter`/`filter` processors, `jaeger_storage`/`jaeger_query`/`healthcheckv2`/`jaeger_mcp` extensions) — one binary for the entire trace path. A separate otelcol core binary handles the log path (the `file` exporter Jaeger doesn't ship). Query latency measured at p95=12.2ms (40× under the 500ms budget). **Agent surface — traces**: Jaeger's bundled `jaeger_mcp` extension is wired directly into every registered project's `.mcp.json` (same pattern as dash0's cloud MCP). The agent gets 8 pre-computed tools (`search_traces`, `get_trace_topology`, `get_span_details`, `get_services`, `get_span_names`, `get_trace_errors`, `get_critical_path`, `health`) without indusk-mcp wrapping them. **Agent surface — logs**: custom `tail_logs` MCP tool in indusk-mcp filters the otelcol JSONL sink by `service` / `level` / `since_minutes` / `limit`. **Human CLI**: `indusk telemetry tail | trace | services | reset` mirror the agent surface for terminal diagnosis. **Required-by-default resolution**: `autoEnableExtensions` split into two passes — Pass 1 iterates required extensions and enables every `required: true` extension not in `.indusk/config.json`'s new `disabled_extensions` array, Pass 2 runs the original detection-based flow. Both `indusk init` and `indusk update` call `autoEnableExtensions`, so fresh scaffolds AND pre-1.28 project upgrades pick up local-telemetry identically. **Escape hatch**: hand-edit `.indusk/config.json` to add `"disabled_extensions": ["local-telemetry"]` — init and update both honor it silently. No CLI affordance; opting out is deliberate and rare. **Hook plumbing fix** (surfaced by Phase 6 tests): `extensionsDisable` now fires `on_disable` BEFORE renaming the manifest dir, symmetrical to how `extensionsEnable` fires `on_init`/`on_enable` — pre-1.28 the CLI silently skipped `on_disable`, so `indusk extensions disable local-telemetry` would rename the manifest but never run `indusk telemetry deregister`, leaving daemons running and `.mcp.json` stale. Also new: `INDUSK_BIN` env var overrides `indusk ` in hook shell commands (test-critical for pre-1.28 global + pre-release tarballs), telemetry registry realpath-normalizes paths (macOS `/var` ↔ `/private/var` phantom-no-match fix), Jaeger's self-metrics disabled via `service.telemetry.metrics.level: none` (port 8888 cross-spawn conflict fix, test flakiness 50% → 0%). Tests: 10 telemetry tests across 5 files (T2/T6/T12/T18/T19/T20/T23 + 3 cases in enable/disable/upgrade). Full architecture + ADR at [`reference/telemetry/overview`](/reference/telemetry/overview), CLI reference at [`reference/telemetry/cli`](/reference/telemetry/cli). Foundation for two downstream plans: `telemetry-watcher-agent` (local-only async observer) and `unified-telemetry-query` (later user-facing Jaeger + Dash0 translation layer).

- **Admin UI remembers collapsible section state across page reloads (1.27.7)** — the Brief section (and every other `<CollapsibleSection>`: Research, Test Plan, ADR, each Phase) no longer re-opens itself on every visit after the user has explicitly closed it. Surfaced by the user mid-dogfood on admin-ui-hosting: "every time I come, the brief is open when I have to close it." Fix: `<CollapsibleSection>` accepts an optional `persistKey` prop; when set, the initial state reads from `localStorage[persistKey]` (encoded as `"1"` / `"0"`) with `defaultOpen` fallback, and every toggle writes back. Key convention is `plan:{planName}:section:{slug}` for top-level sections and `plan:{planName}:phase:{N}` for phase collapsibles — stable across re-renders, scoped per-plan so navigating between plans doesn't clobber state. Without `persistKey`, the component behaves exactly as before (ephemeral, `defaultOpen` on every render). SSR/privacy-mode safe: `typeof window` guard, try/catch around both reads and writes, swallowed exceptions fall through to `defaultOpen`. Admin UI wires `persistKey` into every CollapsibleSection in `PlanDetail` — Research, Brief, Test Plan, ADR, and every phase (pre, falsification, follow-up). Tests: 4 new component tests in `CollapsibleSection.test.tsx` (absent-key fallback, stored "0" overrides defaultOpen=true, stored "1" overrides defaultOpen=false, toggle round-trip writes) + 2 new tests in `PlanDetail.test.tsx` (PlanDetail wires persistKey → pre-seeded "0" renders Brief closed; absent storage renders Brief open). See [admin-ui component-conventions](/reference/admin-ui/component-conventions#collapsiblesection-persistence-1-27-7) for the persistKey key convention + SSR/hydration behavior + testing guidance.

### Fixed
- **Admin UI falsification-phase rendering (1.27.6)** — the `/falsify admin-ui-hosting` dogfood surfaced that the admin UI's `FalsificationSection` only knew about the legacy `falsification.md` file, so plans using the new phase-authoring flow (1.27.4+) rendered "No falsification ritual run for this plan" even when they had a Falsification Phase in impl.md. Fix: new `splitPhasesAroundFalsification()` helper in `apps/indusk-admin/src/lib/phases.ts` splits a plan's phases into pre-falsification / falsification / post-falsification groups by matching the phase title substring "Falsification" (case-insensitive, first-match-wins). `PlanDetail` renders three sibling sections in this order: main `Phases` (pre) → `Falsification` (the phase itself) → `Follow-up Phases` (post, hidden when empty). The enhanced `FalsificationSection` accepts the falsification phase as a prop — when present, it delegates to a new `FalsificationPhaseSection` that renders the phase's trajectory rows as a Hypotheses table (ID / Asserts / State) and the phase's checklist items as a Fix items list. Status badge reads `complete` when all trajectory rows are `passing`/`skipped` AND no unchecked items remain. **Backwards compatible**: when no falsification phase exists in impl.md but a legacy `falsification.md` file is present, the existing log-based rendering still works — the log-based and phase-based paths are mutually exclusive but both supported. New `extractChecklistItems()` helper parses `- [ ]` / `- [x]` lines for the fix-items rendering; deliberately narrow (not a general markdown AST parser). Tests: 8 new unit tests in `phases.test.ts` (5 for the split helper, 3 for the checklist extractor) + 5 new component tests in `PlanDetail.test.tsx` (3 for T27 phase-based rendering, 1 for T27 post-phase absence, 1 for T28 legacy-log backcompat). This release also dogfoods the new rendering: admin-ui-hosting's own impl.md now has Phases 1–6 (regular) → Phase 7 (Falsification) → Phase 8 (follow-up polish, this work), which renders correctly in the new layout.

### Fixed
- **Admin UI daemon + registry + cwd-walk-up hardening (1.27.5)** — four defensive fixes surfaced by the `/falsify admin-ui-hosting` pass (the first dogfood of the new phase-authoring flow). **(1) Daemon identity verification**: `daemonStatus()` and `daemonStop()` in `apps/indusk-mcp/src/lib/admin/daemon.ts` now gate on a combined `isAlive(pid) && isPortListening(port)` check, not bare `isAlive` alone. After a daemon crash the OS can recycle the PID to an unrelated process (bash, postgres, another vitest); without the port check, `uiStart`'s double-start guard would refuse to spawn and blame a stranger, and `uiStop` would SIGTERM them. On identity mismatch, `daemonStatus` sweeps the stale PID+meta files; `daemonStop` likewise cleans up and returns `stopped:true` without signaling. **(2) Registry quarantine**: `readRegistry()` in `apps/indusk-mcp/src/lib/admin/registry.ts` now renames a malformed `projects.json` to `projects.json.corrupt.{ISO}.bak` before returning `emptyRegistry()`, so the next `writeRegistry` creates a fresh file without silently overwriting the damaged bytes. Previously, hand-editing a registry typo could destroy every registered project with no recovery path; now the original contents are preserved alongside the fresh file, and a stderr warning names the backup path. **(3) Cwd walk-up in bare `indusk ui`**: `resolveOpenPath()` now walks up cwd's parents (capped at 40 ancestors) to find the nearest registered project, matching realpath against each registered project's realpath. Previously the function did an exact-string compare against the project's root, so `cd apps/indusk-mcp && indusk ui` silently fell through to `/` instead of opening the project's page — regressing T16's cwd-aware promise for any invocation deeper than the project root. **(4) Test-mock fix**: `apps/indusk-admin/src/app/p/[project]/page.test.tsx` was missing `readProjectResearch` in its `@/lib/planning-reader` mock, so the test file failed to load after Phase 6 added `readProjectResearch` to the per-project layout. Phase 6's regression check relied on `tsc --noEmit`, which doesn't import the test file; added to the mock and the file loads. Tests: `apps/indusk-mcp/src/lib/admin/__tests__/daemon-identity.test.ts` (2 cases), `registry-quarantine.test.ts` (2 cases), `apps/indusk-mcp/src/__tests__/resolve-open-path-walkup.test.ts` (4 cases). 8 new green assertions total; all Phase 1–6 admin-ui and indusk-mcp admin tests still pass. This release is the first landing of a Phase authored by `/falsify` under the new 1.27.4 phase-authoring flow.

### Changed
- **`/falsify` becomes a phase-authoring action (1.27.4)** — the falsification ritual no longer runs tests inline; it investigates the plan, forms specific hypotheses, and appends a new Falsification Phase to the plan's `impl.md` with trajectory rows for the hypothesis tests + implementation items for the fixes + standard Verification / Context / Document gates. The plan's impl status stays `in-progress`; `/work` picks up the phase later like any other phase. `/retrospective`'s Step 0 gate gains a third pass condition: "all impl phases terminal" (alongside the existing legacy `isFalsificationComplete` check and the `falsification: skipped` + reason frontmatter path). **Backwards compatible** — plans authored under the old ritual that have completed `falsification.md` files continue to work unchanged; the `apps/indusk-mcp/src/lib/falsification/log.ts` library is kept in place for legacy reads. **Motivation**: the old sidecar-log shape was invisible in admin UI (phases render; log files don't), and forced immediate test execution (no way to capture hypotheses now and execute later). The new shape makes falsification visible (phases render), deferrable (capture now, run later), and traceable (the plan's phase sequence shows normal work → falsification → fix phases → close). Also removes the three-outcome-per-hypothesis branching ("fix in scope / spawn new plan / accept as finding") — in-scope fixes become items in the Falsification Phase, which is the new default; "spawn new plan" stays available for genuinely large gaps but isn't decided inside the ritual; "accept as finding" is gone (findings that deserve record either become phase items or surface in retrospective, not a sidecar log). Skill files updated: `apps/indusk-mcp/skills/falsify.md` + `apps/indusk-mcp/skills/retrospective.md` (+ mirrored to `.claude/skills/*/SKILL.md`); guide doc `apps/indusk-docs/src/guide/falsification-ritual.md` rewritten with phase-authoring walkthrough + legacy-plans section. Regression test `apps/indusk-mcp/src/__tests__/retrospective-gate-backcompat.test.ts` locks in that legacy `falsification.md` plans still pass the gate (3/3 assertions). Three pending retros (`admin-ui-hosting`, `indusk-admin-ui`, `eval-agent-mcp-access`) will be the first real uses of the new flow.

### Fixed
- **Remove stale top-level nav links from admin UI header (1.27.3)** — 1.27.2 removed the `/scorecards` cross-project route but left the global header in `apps/indusk-admin/src/app/layout.tsx` with `Projects` and `Scorecards` links, and `Scorecards` now 404s. The header's two `<Link>`s are both removed; the header becomes pure chrome (title + "read-only viewer" subtitle on the left, nothing on the right). All navigation now lives in the per-project sidebar (`/p/{project}/` → Scorecards link + plan list + Research group) where it belongs — top-level nav was inherited from the 1.26.0 shell and had no remaining purpose after project-siloing. Surfaced immediately at smoke after 1.27.2 shipped.

### Changed
- **Admin UI scorecards project-siloed + research section + brief collapsible (1.27.2)** — three UX polish items batched into a Phase 6 follow-up after the 1.27.x daemon model proved stable under smoke. **(1) Scorecards project-siloed**: the cross-project `/scorecards` route from 1.26.0/1.27.0 is **removed** (top-level `/scorecards` now 404s). New per-project route at `/p/{project}/scorecards` reads only that project's `.indusk/eval/results.log`, with a "Scorecards" link added to the per-project sidebar. Bookmark break with no replacement — cross-project view is deliberately out of scope going forward; per-project is the canonical view. **(2) Research section**: new `/p/{project}/research/{slug}` route backed by `.indusk/research/` — renders top-level `.md` files or `{slug}/README.md` for nested dirs, via the same `<Markdown>` component used by plan pages. Per-project sidebar gains a "Research" group listing every slug (with parsed `# H1` as title when present). Empty state omits the group entirely when `.indusk/research/` is missing or empty. Two new readers in `planning-reader.ts`: `readProjectResearch(projectRoot): ResearchEntry[]` and `readResearchContent(projectRoot, slug): string | null` — the latter rejects path-traversal (`..`, `/`, leading `.`). **(3) Brief collapsible**: the Brief section on plan detail pages is now wrapped in `<CollapsibleSection defaultOpen={true}>` for parity with Test Plan + ADR. Click the chevron to collapse — the outer `data-testid="brief-section"` is preserved so existing T6 assertions still work. Tests: 4 new T19 HTTP assertions (`http-project-scorecards.test.ts`), 5 new T20 HTTP assertions (`http-project-research.test.ts`), 2 new T21 component assertions in `PlanDetail.test.tsx`. The Phase 4 `http-scorecards-cross-project.test.ts` file was deleted alongside the feature it tested; T15 stays `passing` in the trajectory (historically accurate at Phase 4 close) with an inline "superseded by T19 in Phase 6" annotation.

### Added
- **`indusk ui restart` subcommand (1.27.1)** — stops the running daemon (if any) and starts a fresh one in a single command. Picks up a new admin bundle after `npm i -g @infinitedusky/indusk-mcp@<newer>` without forcing users to chain `indusk ui stop && indusk ui start`. Honors `--port` / `--no-open` via `optsWithGlobals()` just like `start`. If no daemon is running, the stop half prints `Admin UI is not running.` and the start half runs normally — equivalent to a plain `start`. `start` intentionally continues to no-op when a daemon is already running (T4's contract), so `restart` is the load-bearing path for upgrade flows. Two new tests in `admin-cli-lifecycle.test.ts` cover the running-daemon case (new PID returned, stop + start both in output) and the no-daemon case (no-op stop, fresh start). Surfaced while smoke-testing 1.27.0 on dusk — old 1.26.0 CLI processes (which didn't daemonize and ran `next dev` in the foreground) had to be killed by hand before 1.27.0 could take port 3939, and there was no one-command recovery.

### Added
- **Admin UI hosted as a single long-lived daemon (1.27.0)** — replaces the broken per-project `indusk ui` model from 1.26.0. The admin UI is now served by one native Node daemon that covers every InDusk project on the machine, not a new Next.js instance per project. New CLI shape: `indusk ui start` spawns the daemon (detached, survives terminal close), `indusk ui stop` SIGTERMs it, `indusk ui status` reports running/port/registered-project count, and bare `indusk ui` is aliased to `start` with cwd-aware browser open (inside a registered project → `/p/{this-project}/`, outside → `/`). Daemon metadata lives at `~/.indusk/admin-ui.{pid,json,log}`. Multi-project via `~/.indusk/projects.json` registry, populated transparently by `indusk init` (appends) and `indusk update` (validates + touches). Routes restructured to `/p/[project]/...` with `/` as the project grid (one card per registered project, each linking into its namespace) and `/scorecards` walking every registered project's `.indusk/eval/results.log` with project labels and cross-project sort. Stale/deleted registry entries render a 200-page `StaleProjectFailurePage` with a "needs reconfiguration" recovery hint (`cd` + `indusk update`), never 500 or 404. Bundling variant A3: `prepublishOnly` runs `pnpm build && pnpm --filter indusk-admin build && node scripts/bundle-admin.js`, copying the pre-built Next.js production output into `apps/indusk-mcp/admin/` and shipping it in the tarball — consumers need zero extra tooling (no `pnpm install`, no `next build`, no workspace assumption). Tarball size ~12 MB (cap 50 MB). **Breaking change from 1.26.0**: the per-project `indusk ui` is removed; 1.26.0 users must run `indusk init` once per project to register (or `indusk update` for already-init'd projects), then `indusk ui start` from anywhere. Three implementation gotchas baked into CLAUDE.md: (1) `commander@13` silently drops duplicate options when declared on both parent + child subcommands — options live only on the parent, subcommands read via `this.optsWithGlobals()`; (2) vitest `fileParallelism: false` in the node project because multiple HTTP smoke tests each spawn `next dev` and parallel CPU contention makes `next dev` miss its "Ready in" stdout within 30s; (3) `resolveOpenPath()` normalizes registry paths and `process.cwd()` via `safeRealpathSync` because macOS `mkdtempSync` returns `/var/folders/...` while `process.cwd()` returns `/private/var/folders/...` — raw string compare silently misses. See [Admin UI Overview](/reference/admin-ui/overview) and [Admin UI CLI](/reference/admin-ui/cli) for the daemon model, routing tree, env vars, and recovery procedures.

### Added
- **`indusk ui` command + indusk-admin app (1.26.0)** — first Arc 1 demo asset live. New CLI subcommand `indusk ui [--port <n>] [--no-open]` that spawns the bundled `apps/indusk-admin/` Next.js app pointing at the project root, auto-picking a free port when the requested one is taken. Admin app is a read-only viewer over `.indusk/planning/` and `.indusk/eval/results.log`: sidebar lists active plans in `master.md` order with archived plans in a collapsed section; main pane (per `/plan/[name]` route) renders header, brief, test-plan, ADR, phases-with-trajectory-tables, falsification log (outcome-color-coded), and eval scorecards (date-range overlapped to the plan's lifetime). Reuses indusk-mcp's parsers via workspace subpath exports (`@infinitedusky/indusk-mcp/trajectory/parser`, `@infinitedusky/indusk-mcp/falsification/log`) — never duplicates parsing logic. Custom Tailwind 4 primitives (Button, Badge, Table, CollapsibleSection, Sidebar — no shadcn-ui, no Radix) with structural enforcement via a grep-based component-reuse audit. Tests: 61 across vitest browser-mode (real Chromium via @vitest/browser-playwright) for components and node-mode for the data layer. Bundling for v1 ships the admin app as a workspace dependency (dev mode on the user's machine); v2 may switch to a built static export. See `apps/indusk-docs/src/reference/admin-ui/` for component conventions and the manual smoke procedure at `apps/indusk-admin/test-fixtures/manual-smoke.md`.

### Fixed
- **`rationale_baseline` frontmatter regex anchored to line start (1.25.1)** — surfaced by the `/falsify` ritual against `rationale-baseline-frontmatter` immediately after 1.25.0 shipped: the JS hook port's `/rationale_baseline:\s*(\d+)/` regex was not line-anchored, so a frontmatter whose `title:` value happened to contain the literal substring `rationale_baseline: 1` (e.g., a documentation plan about the key itself) silently inherited that baseline from the title's substring. A Phase-1 row that should have required rationale would silently pass. Fix: changed to `/^rationale_baseline:\s*(\d+)/m` — the `m` flag + `^` anchor require the key to appear at the start of a line, i.e., as a top-level YAML key. Mirrored across `apps/indusk-mcp/hooks/validate-impl-structure.js` and `.claude/hooks/validate-impl-structure.js`. Two regression tests in `apps/indusk-mcp/src/__tests__/rationale-baseline-falsify-substring.test.ts`: substring-only attack must be rejected (validator exits 2), legitimate top-level key must still be honored (validator exits 0). The same unanchored-regex shape exists for sibling keys (`gate_policy:`, `rationale:`, `trajectory:`, `workflow:`) but those are presence/enum checks where false-positive risk is far lower than for an integer-valued key — intentionally out of scope. Demonstration of the falsification ritual catching a real bug introduced one publish ago — exactly the discipline pattern that `eval-scorecard-format-fix` retro identified as load-bearing.

### Added
- **Trajectory `rationale_baseline` frontmatter key (1.25.0)** — plans can declare their writable baseline via `rationale_baseline: N` (integer, default `0`). Rows with `Writable at <= baseline` are exempt from the `### Trajectory Rationale` subsection. Use when Phase 1 IS the enabling work — refactors, schema migrations, scaffolding plans where "writable today against the current stack" is meaningless because the current stack is what's being replaced. Default `0` preserves all existing plan behavior; the change is purely additive. The validator's two error messages are baseline-aware (`"later than Phase ${baseline}"` instead of hardcoded `"Phase 0"`), so operators see the rule the plan declared, not the global default. Mirrored across TS source (`apps/indusk-mcp/src/lib/trajectory/validator.ts`), JS hook port (`apps/indusk-mcp/hooks/validate-impl-structure.js`), and project install (`.claude/hooks/validate-impl-structure.js`). 5 unit tests + 5 subprocess-based parity fixtures (`apps/indusk-mcp/src/__tests__/rationale-baseline-parity.test.ts`) verifying TS source ↔ JS hook port produce identical pass/fail decisions on shared input. Surfaced from Numero's `table-lifecycle-unification` plan: 41 of 44 trajectory rows were `Writable at: Phase 1` because Phase 1 was "rename DB tables + author compat views" — tests couldn't be written against pre-migration state without literally reverting production code. The hook nevertheless demanded rationale for all 41 rows, fired on every Edit to impl.md, and agents routed around it via Write-based heredocs (which bypass `PreToolUse:Edit` entirely). The rail-integrity problem: a gate designed to catch real authoring mistakes fired on legitimate usage and taught the agent to ignore it. With this fix, Numero's three queued plans (`restart-recovery`, `coc4-verification-debt-audit`, `drop-compat-views`) — each with Phase 1 as enabling work — can be authored cleanly. Docs: new "Trajectory Rationale and the `rationale_baseline` key" section in `apps/indusk-docs/src/guide/test-trajectory.md` documenting the key, the previously-undocumented `rationale: required` opt-in, and a frontmatter key reference table.

### Fixed
- **All `scorecard.questions` iteration sites use `getScorecardQuestions` helper (1.24.5)** — 1.24.4 fixed `findings.ts:69` but missed `persistent-evaluator.ts:338`, which still used `for (const q of scorecard.questions ?? [])`. Surfaced on Numero 2026-04-19 19:54 UTC: model returned `"questions": { "conventions": {verdict, commentary} }` — an OBJECT keyed by question id, not an array. `?? []` only triggers for null/undefined, so `{} ?? []` returns `{}`, and `for...of {}` throws `TypeError: object is not iterable`. **Live demonstration of the cheat-sheet effect** in our just-shipped `/falsify` ritual: I focused on the iteration site I knew about (findings.ts) and didn't grep for ALL iteration sites — exactly the failure mode `falsify-spawn-pattern` (master.md plan #10) is designed to prevent. Fix: extracted `getScorecardQuestions<T>(scorecard)` helper in `apps/indusk-mcp/src/lib/eval/scorecard-extractor.ts` (single source of truth for malformed-scorecard defenses; `Array.isArray` check, returns `[]` for any non-array shape). Replaced both call sites (`findings.ts:69`, `persistent-evaluator.ts:338`) with the helper. New test `does not throw when scorecard.questions is an OBJECT keyed by question id` covers the exact Numero failure shape.
- **`ingestScorecard` tolerates malformed scorecard shape (1.24.4)** — surfaced on Numero 2026-04-19 via the falsification ritual against `eval-scorecard-format-fix`: the model occasionally returns a scorecard with a completely invented schema (flat fields like `commit`, `description`, `skipped_steps`, `followed_conventions` instead of `{questions: [...], summary, graphitiWrites}`). The wrapper successfully JSON-parsed it and wrote it to `results.log`, then crashed inside `ingestScorecard` at `findings.ts:69` because the `for (const q of scorecard.questions)` loop had no array-fallback guard. The crash produced a misleading `error: true` entry RIGHT AFTER the (wrong-shape) scorecard was already written — two entries per changeId, the second one falsely implying the scorecard was lost. Fix: replaced the unguarded iterate with `Array.isArray(scorecard.questions) ? scorecard.questions : []`. Now malformed-shape scorecards land in `results.log` (where they can be inspected for what the model actually returned) without producing a spurious error entry. 4 vitest tests in `__tests__/ingest-scorecard-malformed.test.ts` cover the failure modes (missing field, null, non-array boolean, well-shaped regression). Discovered by `/falsify` (which itself was found to have the cheat-sheet effect — see master.md plan #10 `falsify-spawn-pattern` for the structural redesign).

### Fixed
- **Eval scorecard timestamps reflect actual completion time (1.24.3)** — the scorecard's `timestamp` field was being filled in by the LLM (per the prompt: `"timestamp": "{ISO 8601 now}"`) but the LLM doesn't know the real current time and tends to round to convenient values like 5-minute marks (e.g. `2026-04-19T18:25:00Z`). The wrapper code overrode `usage` and `telemetryPosted` after parsing the scorecard but didn't override `timestamp` — so the model's guess survived to disk. Reading `results.log` showed timestamps clustered at suspiciously round times. Fix: override `scorecard.timestamp = new Date().toISOString()` immediately after `JSON.parse` in all 3 spawn paths (persistent-evaluator's resume + fresh, evaluator-runner's background + sync). The wrapper knows the actual completion time; use it. Now timestamps in `results.log` are accurate to the millisecond.

### Changed
- **Work skill: describe-then-do order is load-bearing (1.24.2)** — work skill's commit guidance restructured to make the order explicit and unmissable: `jj new` → `jj describe -m "..."` (intent in present/future tense) → do the work → check item off → repeat. Added an explicit anti-pattern callout naming WHY describe-after-do is broken: (a) the eval agent fires on `jj describe` and scores against whatever description is current at that moment, so late descriptions mean the agent scores work that's already done with no intent context; (b) the working copy IS the current commit in jj, so any uncommitted state automatically gets attributed to the previous unit's description if you don't `jj new` first. Surfaced from observation that agents on Numero were sometimes describing after-the-fact and inheriting wrong commit attribution. Single-skill change in `apps/indusk-mcp/skills/work.md` + sync.

### Changed
- **Work skill defaults to per-item commits, not per-phase (1.24.1)** — surfaced from a Numero refactor where 10-item phases were producing single sprawling commits that needed `jj split` at phase close. The work skill's commit guidance previously said "typically per checklist item OR per phase gate" — that `or` gave agents license to batch. Tightened to: **one commit per checklist item is the default**; phase-close commits are the exception, only when items are trivially related (e.g., a phase that's "rename X → Y in 5 files"). Cost is not a reason to batch — eval session-resume after the first commit amortizes the catchup, so per-item commits are cheap. Single-line behavioral change in `apps/indusk-mcp/skills/work.md` + sync to `.claude/skills/work/SKILL.md`. Result: cleaner blame, useful bisect, no end-of-phase split chore, and the eval agent gets to score each unit while context is fresh.

### Fixed
- **Eval scorecard parser tolerates prose-mixed JSON output (1.24.0)** — surfaced on smoke 4 of `eval-agent-mcp-access`: the evaluator's MCP work succeeded (3 highlights processed end-to-end into Graphiti episodes), but its final scorecard output was prose-prefixed (`"Now I've got everything... {scorecard}"`) and `JSON.parse(stdout)` choked on the leading prose. Result: an `error: true` entry landed in `.indusk/eval/results.log` even though the eval agent's actual work completed correctly — the system silently under-counted itself. Fix: extracted scorecard JSON parsing into `apps/indusk-mcp/src/lib/eval/scorecard-extractor.ts` with a third extraction strategy (balanced-brace scan with string-literal awareness, so `{` inside JSON string values doesn't fool the depth counter). Strategy order: trim-and-parse → fenced-block regex → balanced-brace scan; first success wins. Used at all 3 `claude --print` call sites in `persistent-evaluator.ts` + `evaluator-runner.ts`. Error-entry messages now include the first 500 chars of raw stdout via `formatParseError(err, rawStdout)` — no more re-running the evaluator just to see what Claude actually wrote. Also tightened the prompt: appended a "FINAL REMINDER — OUTPUT FORMAT" section AFTER the existing Step 7 with ❌/✅ examples and an explicit "first character must be `{`" instruction, closer to where Claude generates output. 19 vitest unit tests in `scorecard-extractor.test.ts` cover all input shapes (T1–T5) plus the prompt change (T6). Live smoke (T7) verified on dusk + Numero post-publish.
- **Eval agent permission-mode = `bypassPermissions` (1.23.2)** — 1.23.0/1.23.1 added `--mcp-config` and `--permission-mode acceptEdits` to all 4 spawn sites, but smoke verification still produced `graphitiWrites: 0`. Manual A/B confirmed: `acceptEdits` only auto-accepts file-Edit operations, NOT MCP tool calls — every `mcp__indusk__*` invocation was landing in `permission_denials` even when servers were loaded. Switched to `--permission-mode bypassPermissions` on all 4 spawn sites. Safety: the `--allowed-tools` whitelist remains the hard scope (Read/Grep/Glob, Bash(jj/git only), mcp__graphiti__*, mcp__indusk__*, mcp__codegraphcontext__* — no Write, no Edit, no arbitrary Bash). `bypassPermissions` only removes the prompt-for-permission gate inside that bounded surface, which is what an unattended subprocess needs. Manual test confirmed `mcp__indusk__get_system_version` returns "v1.23.1" with no `permission_denials` once both flags are present. The principle the fix encodes: limit the scope strictly via allowlist, then give the agent complete freedom inside that scope.
- **Eval agent MCP fix actually shipped + prepublishOnly hook (1.23.1)** — the 1.23.0 release shipped the *old compiled dist* (the TS fix landed in `src/` but `pnpm build` was never run before publish). Smoke-test verification surfaced this immediately: `graphitiWrites: 0` persisted, and grepping the published `dist/lib/eval/persistent-evaluator.js` for `mcp-config` returned zero matches. 1.23.1 is a clean rebuild + republish containing the actual 1.23.0 source change (no source differences between 1.23.0 and 1.23.1 — only the compiled output). Belt-and-suspenders fix: added `prepublishOnly: pnpm build` script to `apps/indusk-mcp/package.json` so `pnpm publish` will fail-fast if the build doesn't succeed and will guarantee fresh dist on every publish. The exact same failure mode cannot recur.

### Fixed
- **Eval agent finally calls MCP tools (1.23.0)** — the `claude --print` subprocess spawned by the eval-trigger hook was missing `--mcp-config .mcp.json`, so it loaded only globally-configured MCP servers (typically `context7`/`tmux`/`playwright`) and was blind to the project's `indusk` / `graphiti` / `codegraphcontext` servers. The `--allowed-tools` whitelist permitted `mcp__indusk__*` etc., but the tools didn't exist in the subprocess at all — `--allowed-tools` controls what may be *called*; `--mcp-config` controls what *exists*. Result: every scorecard logged `graphitiWrites: 0`, `.indusk/highlights-processed.jsonl` was never created, and the agent-roles plan's "eval agent processes highlights into Graphiti episodes" architecture was operationally a no-op since 1.17.0 shipped. Fix: all four `claude --print` spawn sites in `apps/indusk-mcp/src/lib/eval/persistent-evaluator.ts` and `apps/indusk-mcp/src/lib/eval/evaluator-runner.ts` now pass `--mcp-config .mcp.json`. The resume path additionally gains `--permission-mode acceptEdits` (the fresh path already had it; without it, tool calls land in `permission_denials` even when MCP servers are loaded). Diagnosed via direct A/B test of `claude --print` invocations — see `.indusk/planning/archive/eval-agent-mcp-access/diagnosis.md` and the new "MCP tools unreachable" entry under Known Failure Modes in `apps/indusk-docs/src/reference/eval/overview.md`.

### Added
- **Behavioral test plan document between brief and ADR (1.22.0)** — new `test-plan.md` document type added to the planner skill lifecycle for `feature` workflows. Slots between brief acceptance and ADR authoring. Lists the **behavioral assertions** that must be true for the feature to work, plus the **mechanism** by which each will be tested (vitest unit / vitest integration / e2e script / manual user test / etc.) — but NOT the test code. Discipline: assertions must be behavioral (user-visible) not functional (function/type/internal). "User can sign in with Google" not "googleAuth() returns a JWT." If a non-engineer stakeholder couldn't read an assertion without you explaining a function name, rewrite it. The test plan becomes the source rows for the impl's Test Trajectory: one trajectory row per assertion, with `Writable at`/`Passes at` columns added during impl authoring. Lifecycle now: `research → brief → test-plan → adr → impl → retrospective`. Optional first-class skip: bugfix/refactor workflows continue to skip the test plan; feature workflows include it by default. Will be evaluated on the next Numero feature; if it adds friction without payoff, easy to remove (single planner skill change).
- **Trajectory rationale validator + earliest-writable discipline (1.21.1)** — opt-in fifth trajectory rule. Every trajectory row whose `Writable at` is later than Phase 0 must have a matching entry in a `### Trajectory Rationale` subsection naming what prevents authoring the test before plan code lands. Phase 0 (the pre-plan baseline — "writable today against the current stack") is the default and needs no rationale, so the subsection only fills with the rows that actually require justification. Enable per impl with `rationale: required` in frontmatter; the planner skill template now sets it by default for new impls. The rationale-quality test asks *does this describe a compile error against today's symbols, or does it describe an uninteresting failure mode?* If the latter, the row is a rubber-stamp — move it to Phase 0 (red-for-uninteresting-reason is the whole point of an early-writable test, since it stays red through every intermediate phase as a tripwire). Legitimate Phase 1+ rationales: TypeScript imports of not-yet-exported symbols, constructor signatures that don't yet accept the test's args, enum values not yet defined. Validator rule `rationale-completeness` flags missing Phase 1+ entries and stale entries; mirrors across `.claude/hooks/validate-impl-structure.js` (write-time gate), `apps/indusk-mcp/hooks/validate-impl-structure.js` (package source for `indusk update` to propagate), and `apps/indusk-mcp/src/lib/trajectory/validator.ts` (TS source). 7 new tests (T13–T16 + composite cases + Phase 0 exemption) in validator.test.ts. New lesson `test-red-at-earliest-writable-phase.md` codifies the rule, the rationale-quality test, and the compile-error-vs-rubber-stamp framework.
- **ADR template now opens with a Goal section (1.21.1)** — every new `adr.md` now includes a `## Goal` section immediately before `## Y-Statement`. One bolded sentence states the headline outcome in plain language ("what will be true when this ADR's decisions ship that isn't true today"), followed by a 2-4 sentence paragraph grounding the goal in user-visible terms and naming at least one current failure the decision fixes. The Y-statement's seven canonical clauses remain unchanged — the Goal lets a reader skim the headline without hunting through them. Ships via the planner skill template at `apps/indusk-mcp/skills/planner.md`; `indusk update` copies it into each project's `.claude/skills/planner/SKILL.md`.

### Added
- **Eval agent OTel logs signal (1.19.0)** — the evaluator now emits OTel LogRecords alongside span traces. Content captured: full prompt body (`eval.event=prompt`), Claude subprocess stdout (`eval.event=claude.stdout`), full scorecard JSON (`eval.event=scorecard`), and error stack traces (`eval.event=error`) in the error path. Logs ingest to the same Dash0 dataset as spans (default `agent`, same routing) and auto-correlate with the active span via trace_id + span_id. Root `eval.run` span also gains content-rich attributes: `scorecard.status` (`ok`/`error`), `scorecard.question_count`, `scorecard.summary` (first 500 chars), `scorecard.cost_usd`, `scorecard.duration_ms`, `scorecard.input_tokens`, `scorecard.output_tokens`, `scorecard.answers.{yes,no,partial}`. Verified end-to-end: `dash0 logs query --dataset agent` returns the 3 log records per run after a real evaluator invocation. See the [Eval Agent OTel docs](/reference/tools/otel#eval-agent-otel-opt-in) for the full signal set.

### Fixed
- **Eval agent hook-spawn silent crash (1.19.1)** — the PostToolUse hook at `apps/indusk-mcp/hooks/eval-trigger.js` spawned the evaluator as `node --input-type=module -e <inline-script>` where the inline script's first two statements used CJS `require()` for fs and path. `require` is undefined in ESM scope → `ReferenceError` at parse, line 2, before any user code ran. `stdio: "ignore"` swallowed the stderr. **Every hook-spawned evaluator since at least 2026-04-11 crashed silently** — scorecards stopped appearing in `.indusk/eval/results.log`, the system looked idle, nothing surfaced. Direct invocations (via `indusk eval` subcommands and ad-hoc node scripts) were unaffected. Fix: inline script now uses ESM-native `import { mkdirSync, appendFileSync } from "node:fs"` and `import { dirname, join } from "node:path"`. Belt-and-suspenders: added `process.on("uncaughtException")` + `process.on("unhandledRejection")` handlers that write an `error: true` entry to `results.log` before `process.exit(1)` — so the next silent-failure class is loud. Diagnosed via the predecessor OTel plan's falsification ritual; regression test grep-asserts the fix. See the [eval overview's Known Failure Modes section](/reference/eval/overview#known-failure-modes) for the debugging recipe.
- **Eval agent dataset routing via composable.env (1.18.2)** — when `OTEL_EXPORTER_OTLP_HEADERS` env var is set with a `Dash0-Dataset=<project-dataset>` entry (composable.env default), exporter-constructor headers lose — OTel spec says env beats constructor. Fixed by (1) adding `EVAL_AGENT_DATASET` env var to the dataset resolution chain (priority: `INDUSK_EVAL_OTEL_DATASET` > `EVAL_AGENT_DATASET` > config `eval.otel.dataset` > `"agent"` default), and (2) rewriting `Dash0-Dataset=<old>` to `Dash0-Dataset=<eval-agent-dataset>` in `OTEL_EXPORTER_OTLP_HEADERS` in-place before the exporter reads it. Confirmed end-to-end via `dash0 spans query --dataset agent`: `final-smoke-via-ce-env` span landed in the agent dataset with `EVAL_AGENT_DATASET=agent` + `DASH0_DATASET=indusk-test` in the shell env. See `env/components/dash0.env` for the composable.env wiring.
- **Eval agent OTel header env parsing (1.18.1)** — the OTel SDK's `OTEL_EXPORTER_OTLP_HEADERS` env parser silently drops Bearer tokens that contain a space (e.g., `Authorization=Bearer auth_xxx`). Result: exports retry-looped to no effect even though `initEvalOtel` logged success. Fixed by reading `DASH0_API_TOKEN` directly in `otel.ts` and attaching `Authorization: Bearer ${DASH0_API_TOKEN}` to the exporter constructor's `headers` option. User-set `OTEL_EXPORTER_OTLP_HEADERS` env still takes precedence per OTel spec. Diagnosed via `dash0 spans query` showing our test probe reached Dash0 only when headers were hardcoded in the constructor.

### Added
- **Eval agent OpenTelemetry (1.18.0)** — opt-in OTel tracing for the background eval agent (evaluator). Default OFF, zero cost in normal operation. Enable via `eval.otel.enabled: true` in `.indusk/config.json` or `INDUSK_EVAL_OTEL=1` env var. Exports to `OTEL_EXPORTER_OTLP_ENDPOINT` (Dash0 or any OTLP HTTP receiver). Routes to Dash0 "agent" dataset by default via `Dash0-Dataset` header; override with `eval.otel.dataset` config or `INDUSK_EVAL_OTEL_DATASET` env. Span tree: root `eval.run` (attrs: `changeId`, `source`, `mode`, `projectGroup`, `resumed`, `highlights.unprocessed_count`) + wrapper-level children (`read_session`, `build_prompt`, `spawn_claude`, `parse_output`, `update_session`, `write_scorecard`, `clear_stale_session`). `eval.spawn_claude` carries `exit.code` + `exit.stderr_tail` (on error) — the primary diagnostic signal when the evaluator fails. Inside-Claude steps (catchup, read_transcript, answer_rubric, etc.) can't be spanned from the wrapper — documented as future work if Claude Code exposes its own OTel. See the [Eval Agent OTel docs](/reference/tools/otel#eval-agent-otel-opt-in).

### Changed
- **Rename: "judge" → "evaluator" / "eval agent" (Phase 0 of `improvement-eval-agent-open-telemetry`)** — the background process that scores every `jj describe` is now called the eval agent or evaluator in code. Files renamed: `judge-runner.ts` → `evaluator-runner.ts`, `persistent-judge.ts` → `persistent-evaluator.ts`. Symbols renamed: `runJudgeSync` → `runEvaluatorSync`, `runJudgeBackground` → `runEvaluatorBackground`, `buildJudgePrompt` → `buildEvaluatorPrompt`, `JudgeRunOptions` → `EvaluatorRunOptions`. Log strings, skill docs, and reference pages updated. The state file `.indusk/eval/judge-session.json` becomes `.indusk/eval/evaluator-session.json` (harmless — the evaluator has been silently failing since 2026-04-11 so no one had live session state). No external CLI or MCP API change. Historical mentions of "judge" in archived changelog entries and archived plan docs remain as-is.

### Added
- **Agent Roles — highlights queue and eval-processing pipeline (1.17.0)** — the working agent stops writing Graphiti episodes at trigger points. Instead it calls `mcp__indusk__highlight` with a tag, a single-line note, and a level (`critical` / `important` / `note`). Highlights append to `.indusk/highlights.jsonl`. The eval agent, spawned on every `jj describe` and at session end, reads unprocessed highlights via `highlights_unprocessed`, writes level-weighted Graphiti episodes via `graph_capture` (critical → 1.0, important → 0.6, note → 0.3), and calls `highlight_mark_processed` with either `wrote-episode` or `skipped`. Three-tier agent roles (working agent / eval agent / infrastructure) documented in CLAUDE.md Architecture + Key Decisions. New `/highlight` slash command for explicit user-flagged moments. `handoff` skill fires `eval-trigger.js --source handoff` at session end so queued highlights are processed before the session ends — the eval-trigger hook now accepts `--source <tag>` and propagates it to the judge via `INDUSK_EVAL_SOURCE`. planner / work / retrospective skills migrated from direct `graph_capture` calls to `highlight` calls. See the [Highlights reference](/reference/tools/highlights) for the full flow and level semantics.

### Changed
- **Repo renamed from `infinitedusky` to `dusk`** — removed `indusk-portfolio` app (will be rebuilt in a separate repo). Updated graph namespaces, config, and docs references. npm package `@infinitedusky/indusk-mcp` unchanged.

### Fixed
- **CLI commands walk up to the project root (1.16.1)** — every non-`init` `indusk` command (`update`, `extensions *`, `init-docs`, `check-gates`, `pr-clean`, `pr-restore`, `graph *`, `eval *`, `beam`, `install`) now walks up from cwd looking for `.indusk/config.json` and errors out with a helpful message if no project is found. Fixes accidental installs into sub-app `.claude/` dirs when the user runs e.g. `indusk update` from inside `apps/indusk-mcp/`. `init` still uses raw cwd (it creates the marker). `.gitignore` now also excludes `apps/*/.claude/` and `apps/*/.indusk/` as belt-and-suspenders.
- **`indusk update` hook sync (1.15.1)** — the update command now discovers bundled hooks via `glob` from the package's `hooks/` dir (instead of a hardcoded list), creates `.claude/hooks/` if missing with all bundled hooks copied in, and logs the source path for debugging. New hooks added to the package now sync on update without code changes to `update.ts`.

### Added
- **Falsification Ritual (1.16.0)** — new `/falsify {plan}` skill between `/work` completion and `/retrospective`. Same working agent, goal-flipped from "prove it works" to "find a failing test." Drives a bounty-hunting loop: investigate the code, form a specific hypothesis about what should be broken, write the test that confirms it, run it. Three outcomes per failing test: fix in scope (reopens the impl with a new phase), spawn a new plan, or accept as finding. Hybrid exit (agent proposes termination, user confirms). Retrospective skill's Step 0 hard-blocks without either a completed `falsification.md` log or `falsification: skipped` + `falsification_reason` in the impl frontmatter. Bookend to the Test Trajectory. See the [Falsification Ritual guide](/guide/falsification-ritual).
- **Test Trajectory (1.15.0)** — every new impl.md opens with a `## Test Trajectory` table (`ID | Asserts | Writable at | Passes at | State`), and phase Verification sections reference test IDs rather than restating checks. Deferred Verification handles untestable items with three required fields (`reason`, `would require`, `mitigation`). Four validator rules enforced by `validate-impl-structure.js`; phase close structurally gated by `check-gates.js` blocking advance when `Passes at: Phase N` rows aren't `passing`/`skipped`/`blocked`. Retrospective skill audits mitigations via `auditPlanAtClose`. Enable on an impl with `trajectory: required` in the frontmatter. See the [Test Trajectory guide](/guide/test-trajectory).
- **Local init mode** (`indusk init --local`) — use InDusk on team codebases without touching committed files. Uses `.git/info/exclude` for isolation.
- **`.indusk/config.json`** — central project profile recording mode, detected tooling, and verify contract. Both modes use it.
- **`indusk pr-clean` / `pr-restore`** — strip and re-apply InDusk settings overlay for clean PRs
- **Settings overlay** (`.indusk/settings-overlay.json`) — tracks what InDusk added to `.claude/settings.json`
- Local-mode quality tools: `.indusk/biome.json`, `.indusk/tests/`, `.indusk/docs/`
- Tooling detection at init (linter, test runner, OTel, TypeScript)

- **Context beam** — file-specific context delivery via 6-query pipeline across semantic graph, Graphiti, eval findings, and CGC. Distance-based relevance decay, trace mode for transparency, configurable pipeline. `context_beam` MCP tool and `indusk beam` CLI.

- **Context system evaluation** — commit-triggered judge agent scores every commit against project conventions, lessons, and graph data. Two modes: eval (always on, writes derived insights to Graphiti) and baseline (vanilla agent comparison). `indusk eval summary` for trends, `indusk eval baseline --task <path>` for delta measurement. `/eval review` for manual quality checks.

- **Semantic graph bridge** — per-project event-sourced projection of CGC structure and Graphiti knowledge, versioned via jj change IDs. Anchors for files, functions, classes, interfaces; internal import edges. `indusk graph sync/rebuild/status` CLI and MCP tools. Automatic sync at phase boundaries.

### Changed
- **Planning moved to `.indusk/planning/`** — all modes. `.indusk/` is now the InDusk home directory.
- All skill path references updated (`planning/` → `.indusk/planning/`)
- `indusk update` respects local mode — re-applies overlay after syncing

### Fixed
- Pre-existing test failure: `otel-core-skill` plan reference in plan-parser tests replaced with existing plan

---

- OpenTelemetry extension — auto-instrumentation scaffolding, Pino structured logging, category-based filtering exporter. Every project is observable from `init`.
- Excalidraw extension — hand-drawn diagrams for planning, debugging, and teach mode (complements Mermaid for formal docs)
- Extension directory format — extensions use `{name}/manifest.json` + `.env` instead of flat files, auto-migrates
- ExcalidrawEmbed component — persistent, interactive Excalidraw diagrams in VitePress docs via iframe

## [1.2.9] - 2026-03-24

### Added
- Gate policy enforcement with conversation proof — `ask` mode now enforced by hooks at both write and execution time
- Extensions update command — re-fetches manifest and installs latest npm package automatically
- Post-update hooks — extensions can run commands after being updated
- Documentation Plan section in ADR template — plan changelogs, pages, and diagrams upfront
- Catchup enforcement hook — blocks code edits until `/catchup` completes all steps
- 9 new graph tools wrapping CGC — visualize, doctor, dead_code, complexity, callers, callees, find, watch, stats
- `graph_ensure` tool — validates and auto-repairs the full CGC stack (container, connection, indexing)
- Research: context-graph (semantic layer on code graph) and MCP dashboard (real-time agent activity UI)

### Changed
- CGC connection fast-fail — 2 second timeout instead of 60 seconds
- Auto-detect FalkorDB host — tries `falkordb.orb.local` first, falls back to `localhost`
- Extensions update detects package manager (pnpm/yarn/npm) and handles workspaces

### Fixed
- FalkorDB volume mount path (`/var/lib/falkordb/data` not `/data`)
- npm extension fetch for third-party extensions
- Catchup skill now requires reading all skill files (step 6)

## [1.0.0] - 2026-03-22

### Added
- Extension system — one system, two sources (built-in + third-party manifests)
- 10 built-in extensions (falkordb, cgc, nextjs, tailwind, react, solidity, typescript, testing, docker, vitepress)
- Extensions CLI — list, enable, disable, add, remove, status, suggest
- Extension manifest spec for third-party integrations
- composable.env as first third-party extension

### Changed
- Domain skills replaced by extensions
- All MCP tools refactored to consume extension manifests instead of hardcoded knowledge

## [0.9.1] - 2026-03-22

### Added
- VitePress llms.txt plugin for LLM-friendly documentation
- Teach mode for `/work` — explains before and after every edit
- Gate policy system (strict/ask/auto) for controlling override behavior
- `init-docs` command — scaffolds VitePress with Mermaid, llms, FullscreenDiagram
- Handoff and catchup skills for session continuity
- Onboard skill renamed to catchup

### Changed
- Document skill now workflow-aware — features get full docs, bugfixes update existing pages

## [0.8.0] - 2026-03-21

### Added
- Impl structure validation hook — blocks writing impls with missing gate sections
- Workflow-aware gates — bugfix requires only verification, feature requires all four
- Override formats: `(none needed)`, `skip-reason:`, conversation proof

## [0.6.0] - 2026-03-21

### Added
- Plan execution gate enforcement via Claude Code hooks
- `check-gates.js` — PreToolUse hook blocks phase transitions with incomplete gates
- `gate-reminder.js` — PostToolUse hook nudges after impl edits
- `validate-impl-structure.js` — blocks impls missing required sections

## [0.4.1] - 2026-03-20

### Added
- Deep CGC integration — required graph steps in all skills
- Graph wrapper tools (index_project, query_dependencies, query_graph)
- Update command installs new skills that didn't exist before

### Changed
- FalkorDB port forwarding removed — uses OrbStack hostname `falkordb.orb.local`

## [0.1.0] - 2026-03-20

### Added
- Initial release — CLI (init, update, serve), 14 MCP tools, core parsers
- Plan management tools (list_plans, get_plan_status, advance_plan, order_plans)
- Context tools (get_context, update_context)
- Quality tools (quality_check, suggest_rule, get_quality_config)
- Document tools (list_docs, check_docs_coverage)
- System tools (get_system_version, check_health, get_skill_versions)
- 6 skills (plan, work, verify, context, document, retrospective)
- Biome configuration with quality ratchet
